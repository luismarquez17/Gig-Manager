databases:
  - name: gig-manager-db
    plan: free

services:
  # Servicio Web
  - type: web
    name: gig-manager-web
    env: docker
    plan: standard
    healthCheckPath: "/"
    envVars:
      - key: RAILS_ENV
        value: production
      - key: RAILS_LOG_TO_STDOUT
        value: "1"
      - key: SECRET_KEY_BASE
        generateValue: true
      - key: DATABASE_URL
        fromDatabase:
          name: gig-manager-db
          property: connectionString