<div class="landing-container">

  <%# --- 1. HERO SECTION DINÁMICO SEGÚN PLANTILLA --- %>
  <% case @company.landing_template %>
  <% when "neon_festival" %>
    <%# --- HERO TIPO NEÓN FESTIVAL CYBERPUNK (ASIMÉTRICO / SPLIT) --- %>
    <section id="hero" style="padding: 70px 0 60px; position: relative;">
      <div style="display: grid; grid-template-columns: repeat(auto-fit, minmax(320px, 1fr)); gap: 40px; align-items: center;">
        <div>
          <div class="section-badge" style="letter-spacing: 2px;">[ ⚡ LIVE STAGE CONCERT EXPERIENCE ]</div>
          <h1 class="gradient-title" style="font-size: clamp(2.5em, 5.5vw, 4.4em); line-height: 1.05; margin: 15px 0 20px 0; text-transform: uppercase;">
            <%= @company.landing_hero_title.presence || @company.tagline.presence || "ENERGÍA PURA Y MÚSICA EN VIVO DE ALTO VOLTAJE" %>
          </h1>
          <p style="font-size: 1.1em; color: var(--text-muted); margin-bottom: 30px; line-height: 1.6;">
            <%= @company.landing_hero_subtitle.presence || @company.bio.presence || "Producción escénica de nivel concierto con repertorio bailable y sonido e iluminación de festival." %>
          </p>

          <div style="display: flex; gap: 15px; flex-wrap: wrap;">
            <a href="#cotizador" class="btn-cta-primary">[ ⚡ COTIZAR FESTIVAL EN VIVO ]</a>
            <a href="#galeria" class="btn-cta-secondary">[ 🎬 VER SHOW EN TARIMA ]</a>
          </div>
        </div>

        <div>
          <div class="glass-box" style="padding: 28px; text-align: center; border-width: 2px;">
            <div style="font-size: 4em; margin-bottom: 10px;">🎸⚡🔥</div>
            <h3 style="font-family: 'Space Grotesk', sans-serif; text-transform: uppercase; margin: 0 0 10px 0; font-size: 1.4em; color: white;">
              <%= @company.name %>
            </h3>
            <p style="color: var(--text-muted); font-size: 0.9em; margin-bottom: 20px;">
              Músicos en vivo • Audio Digital • Iluminación Neón
            </p>
            <div style="background: rgba(255,255,255,0.05); padding: 12px; border-radius: 8px; font-weight: bold; color: var(--primary);">
              ⚡ DISPONIBILIDAD TEMPORADA <%= Time.current.year %>
            </div>
          </div>
        </div>
      </div>
    </section>

  <% when "royal_gala" %>
    <%# --- HERO TIPO ROYAL GALA LUXURY & BODAS VIP (EDITORIAL CENTRADO SANCTUARY) --- %>
    <section id="hero" style="text-align: center; padding: 90px 0 80px; position: relative;">
      <div style="font-size: 0.85em; font-family: 'Playfair Display', serif; font-style: italic; color: #fef08a; letter-spacing: 3px; margin-bottom: 14px; text-transform: uppercase;">
        ✨ ──────── 👑 REGALIA &amp; ALTA GALA ──────── ✨
      </div>
      <h1 class="gradient-title" style="font-size: clamp(2.8em, 6vw, 4.8em); line-height: 1.1; margin: 0 0 20px 0; font-family: 'Playfair Display', serif; font-style: italic; max-width: 920px; margin-left: auto; margin-right: auto;">
        <%= @company.landing_hero_title.presence || @company.tagline.presence || "Elegancia Armónica & Shows de Gala Exclusivos" %>
      </h1>
      <p style="font-size: clamp(1.1em, 2.5vw, 1.3em); color: #cbd5e1; max-width: 720px; margin: 0 auto 40px; line-height: 1.7; font-family: 'Plus Jakarta Sans', sans-serif; font-weight: 300;">
        <%= @company.landing_hero_subtitle.presence || @company.bio.presence || "Acompañamos las bodas y recepciones VIP más sofisticadas del país con arreglos musicales sinfónicos, ensamble de gala y distinción absoluta." %>
      </p>

      <div style="display: flex; justify-content: center; gap: 20px; flex-wrap: wrap; margin-bottom: 50px;">
        <a href="#cotizador" class="btn-cta-primary" style="padding: 16px 36px;">
          👑 Presupuestar Gala VIP
        </a>
      </div>
    </section>

  <% when "minimal_acoustic" %>
    <%# --- HERO TIPO MINIMAL STUDIO & ACÚSTICO REFINADO (STUDIO SPEC SHEET) --- %>
    <section id="hero" style="padding: 60px 0 70px; border-bottom: 1px solid rgba(255,255,255,0.1);">
      <div style="display: grid; grid-template-columns: repeat(auto-fit, minmax(300px, 1fr)); gap: 30px; align-items: flex-end;">
        <div>
          <div class="section-badge">● STUDIO RECORDING LIVE</div>
          <h1 class="gradient-title" style="font-size: clamp(2.2em, 5vw, 3.8em); line-height: 1.1; margin: 10px 0 16px 0; font-family: 'Inter', sans-serif; font-weight: 900;">
            <%= @company.landing_hero_title.presence || @company.tagline.presence || "Música en Vivo de Estudio & Acústico Unplugged" %>
          </h1>
          <p style="font-size: 1em; color: var(--text-muted); margin: 0 0 25px 0; line-height: 1.6;">
            <%= @company.landing_hero_subtitle.presence || @company.bio.presence || "Estética minimalista de alta calidad sonográfica. Cero exageración, puro talento y audio prístino." %>
          </p>

          <div style="display: flex; gap: 12px; flex-wrap: wrap;">
            <a href="#cotizador" class="btn-cta-primary">Cotizar Formato</a>
            <a href="#paquetes" class="btn-cta-secondary">Tarifas Base</a>
          </div>
        </div>

        <div class="glass-box" style="padding: 20px; font-family: 'Inter', sans-serif; font-size: 0.88em;">
          <div style="display: flex; justify-content: space-between; border-bottom: 1px solid rgba(255,255,255,0.1); padding-bottom: 8px; margin-bottom: 8px;">
            <span style="color: var(--text-muted);">Proyecto:</span>
            <strong style="color: white;"><%= @company.name %></strong>
          </div>
          <div style="display: flex; justify-content: space-between; border-bottom: 1px solid rgba(255,255,255,0.1); padding-bottom: 8px; margin-bottom: 8px;">
            <span style="color: var(--text-muted);">Formato Base:</span>
            <span style="color: white;">1 Show 60m + 4h Sonido</span>
          </div>
          <div style="display: flex; justify-content: space-between;">
            <span style="color: var(--text-muted);">Calificación:</span>
            <strong style="color: #fbbf24;">★ <%= @rating %> / 5.0</strong>
          </div>
        </div>
      </div>
    </section>

  <% else %>
    <%# --- HERO CLÁSICO ESCENARIO (classic_stage) --- %>
    <section id="hero" style="text-align: center; padding: 60px 0 80px; position: relative;">
      <div class="section-badge">✨ Experiencia Musical de Alto Nivel</div>
      <h1 class="gradient-title" style="font-size: clamp(2.4em, 6vw, 4.2em); line-height: 1.1; margin: 0 0 20px 0; max-width: 900px; margin-left: auto; margin-right: auto;">
        <%= @company.landing_hero_title.presence || @company.tagline.presence || "La Mejor Música en Vivo para tu Boda o Evento" %>
      </h1>
      <p style="font-size: clamp(1.05em, 2.5vw, 1.25em); color: var(--text-muted); max-width: 680px; margin: 0 auto 35px; line-height: 1.6;">
        <%= @company.landing_hero_subtitle.presence || @company.bio.presence || "Transformamos tu celebración con sonido profesional, puesta en escena impecable y un repertorio diseñado a tu medida en #{@company.name}." %>
      </p>

      <div style="display: flex; justify-content: center; gap: 15px; flex-wrap: wrap; margin-bottom: 40px;">
        <a href="#cotizador" class="btn-cta-primary">
          <span>⚡</span> <%= @company.landing_hero_cta_text.presence || "Simular Mi Presupuesto en Vivo" %>
        </a>
        <a href="#paquetes" class="btn-cta-secondary">
          <span>📦</span> Ver Paquetes Base
        </a>
      </div>
    </section>
  <% end %>

  <%# --- 2. PAQUETES DESTACADOS (PRESET BUDGETS) --- %>
  <% if @company.landing_section_visible?(:show_packages) %>
  <section id="paquetes" style="padding: 40px 0 70px;">
    <div style="text-align: center; margin-bottom: 40px;">
      <span class="section-badge">Nuestros Paquetes</span>
      <h2 class="gradient-title" style="font-size: 2.3em; margin: 0;">Opciones Listas para Celebrar</h2>
      <p style="color: var(--text-muted); font-size: 1.05em; margin-top: 8px;">Elige una configuración prediseñada o personaliza cada detalle en nuestro cotizador.</p>
    </div>

    <% if @preset_budgets.any? %>
      <div style="display: grid; grid-template-columns: repeat(auto-fit, minmax(300px, 1fr)); gap: 25px;">
        <% @preset_budgets.each do |preset| %>
          <% is_feat = preset.featured? %>
          <div class="glass-box" style="display: flex; flex-direction: column; justify-content: space-between; transition: transform 0.3s ease, border-color 0.3s ease; <%= is_feat ? 'border-color: var(--primary); box-shadow: 0 0 30px var(--primary-glow);' : '' %>">
            <div>
              <div style="display: flex; justify-content: space-between; align-items: flex-start; margin-bottom: 12px; flex-wrap: wrap; gap: 8px;">
                <h3 style="margin: 0; font-family: var(--font-outfit); font-size: 1.4em; font-weight: 800; color: var(--text-main);">
                  <%= preset.title %>
                </h3>
                <% if preset.badge_text.present? || is_feat %>
                  <span style="background: <%= is_feat ? 'var(--primary-glow)' : 'rgba(255,255,255,0.08)' %>; color: white; font-size: 0.75em; font-weight: 800; padding: 4px 12px; border-radius: 99px; text-transform: uppercase; border: 1px solid var(--card-border-hover);">
                    <%= preset.badge_text.presence || '⭐ Destacado' %>
                  </span>
                <% end %>
              </div>

              <div style="margin: 15px 0 20px 0;">
                <span style="font-family: var(--font-outfit); font-size: 2.2em; font-weight: 900; color: #34d399;">
                  <%= number_to_currency(preset.price, unit: preset.currency == "USD" ? "$" : "Bs. ", precision: 0) %>
                </span>
                <span style="color: var(--text-muted); font-size: 0.9em;"><%= preset.currency %></span>
              </div>

              <p style="color: #cbd5e1; font-size: 0.95em; line-height: 1.6; margin: 0 0 20px 0; white-space: pre-line;">
                <%= preset.description %>
              </p>
            </div>

            <% 
              wa_text = "¡Hola #{@company.name}! Me interesa reservar el combo \"#{preset.title}\" por $#{preset.price.to_i} #{preset.currency} para mi evento. ¿Tienen disponibilidad?"
              wa_link = "https://wa.me/#{@whatsapp_number.gsub(/[^0-9]/, '')}?text=#{ERB::Util.url_encode(wa_text)}"
            %>
            <a href="<%= wa_link %>" target="_blank" class="btn-cta-primary" style="width: 100%; justify-content: center; text-decoration: none; margin-top: 15px;">
              <span>💬</span> Reservar Este Combo
            </a>
          </div>
        <% end %>
      </div>
    <% else %>
      <%# Paquetes de demostración por defecto %>
      <div style="display: grid; grid-template-columns: repeat(auto-fit, minmax(300px, 1fr)); gap: 25px;">
        <div class="glass-box" style="display: flex; flex-direction: column; justify-content: space-between;">
          <div>
            <h3 style="margin: 0; font-family: var(--font-outfit); font-size: 1.4em; font-weight: 800;">Formato Acústico / Íntimo</h3>
            <div style="margin: 15px 0 20px 0;">
              <span style="font-family: var(--font-outfit); font-size: 2.2em; font-weight: 900; color: #34d399;">$400</span>
              <span style="color: var(--text-muted); font-size: 0.9em;">USD</span>
            </div>
            <p style="color: #cbd5e1; font-size: 0.95em; line-height: 1.6;">
              • Dúo / Trío Acústico (Voz, Guitarra/Piano, Percusión)<br>
              • 2 Horas de Música en Vivo<br>
              • Sonido para hasta 80 personas<br>
              • Ideal para recepciones, cócteles o cenas.
            </p>
          </div>
          <button onclick="selectAndScrollToFormat('acoustic', 400, 'Acústico (2 Músicos)')" class="btn-cta-primary" style="justify-content: center; width: 100%;">⚡ Cotizar Este Formato</button>
        </div>

        <div class="glass-box" style="border-color: var(--primary); box-shadow: 0 0 30px var(--primary-glow); display: flex; flex-direction: column; justify-content: space-between;">
          <div>
            <span style="background: var(--primary-glow); color: var(--text-main); font-size: 0.75em; font-weight: 800; padding: 4px 10px; border-radius: 99px; text-transform: uppercase; border: 1px solid var(--card-border-hover);">Más Popular</span>
            <h3 style="margin: 8px 0 0 0; font-family: var(--font-outfit); font-size: 1.4em; font-weight: 800;">Banda Completa Fiesta</h3>
            <div style="margin: 15px 0 20px 0;">
              <span style="font-family: var(--font-outfit); font-size: 2.2em; font-weight: 900; color: #34d399;">$750</span>
              <span style="color: var(--text-muted); font-size: 0.9em;">USD</span>
            </div>
            <p style="color: #cbd5e1; font-size: 0.95em; line-height: 1.6;">
              • Banda Completa 5 integrantes (Vocalistas, Bajo, Batería, Teclados, Guitarra)<br>
              • 2 Horas de Show Bailable &amp; Pop/Rock<br>
              • Sonido e Iluminación LED Automatizada<br>
              • Montaje profesional completo.
            </p>
          </div>
          <button onclick="selectAndScrollToFormat('full_band', 750, 'Banda Completa (5 Músicos)')" class="btn-cta-primary" style="justify-content: center; width: 100%;">⚡ Cotizar Este Formato</button>
        </div>

        <div class="glass-box" style="display: flex; flex-direction: column; justify-content: space-between;">
          <div>
            <h3 style="margin: 0; font-family: var(--font-outfit); font-size: 1.4em; font-weight: 800;">Show Premium Big Band</h3>
            <div style="margin: 15px 0 20px 0;">
              <span style="font-family: var(--font-outfit); font-size: 2.2em; font-weight: 900; color: #34d399;">$1,200</span>
              <span style="color: var(--text-muted); font-size: 0.9em;">USD</span>
            </div>
            <p style="color: #cbd5e1; font-size: 0.95em; line-height: 1.6;">
              • Banda 8+ músicos (Incluye sección de Metales: Trompeta &amp; Saxo)<br>
              • 2 Horas de Show Continuo de Alto Impacto<br>
              • Sonido para grandes aforos<br>
              • Experiencia VIP para bodas y galas.
            </p>
          </div>
          <button onclick="selectAndScrollToFormat('big_band', 1200, 'Big Band con Metales (8+ Músicos)')" class="btn-cta-primary" style="justify-content: center; width: 100%;">⚡ Cotizar Este Formato</button>
        </div>
      </div>
    <% end %>
  </section>
  <% end %>

  <%# --- 3. SIMULADOR / COTIZADOR VISUAL EN VIVO (LÓGICA SEPARADA: SHOWS VS SONIDO) --- %>
  <% if @company.landing_section_visible?(:show_calculator) %>
  <section id="cotizador" style="padding: 40px 0 80px;">
    <div class="glass-box" style="border: 2px solid var(--card-border-hover); box-shadow: 0 12px 50px var(--primary-glow);">
      <div style="text-align: center; margin-bottom: 35px;">
        <span class="section-badge">Calculadora en Vivo</span>
        <h2 class="gradient-title" style="font-size: 2.4em; margin: 0;">
          <%= @company.landing_calculator_title.presence || "Simula tu Presupuesto al Instante" %>
        </h2>
        <p style="color: var(--text-muted); font-size: 1.05em; margin-top: 8px;">
          <%= @company.landing_calculator_subtitle.presence || "Elige tu formato, cantidad de pases de show y horas de sonido para tu fiesta." %>
        </p>
      </div>

      <div style="display: grid; grid-template-columns: 1.3fr 1fr; gap: 35px; align-items: start;">
        
        <%# Controles del Simulador %>
        <div>
          <%# 1. Formato de Banda %>
          <div style="margin-bottom: 25px;">
            <label style="display: block; font-family: var(--font-outfit); font-weight: 800; font-size: 0.95em; text-transform: uppercase; color: var(--primary); margin-bottom: 12px;">
              1. Selecciona el Formato de la Agrupación
            </label>
            <div style="display: grid; grid-template-columns: repeat(auto-fit, minmax(140px, 1fr)); gap: 10px;">
              <% @calculator_formats.each_with_index do |fmt, idx| %>
                <div class="sim-pill <%= 'active' if idx == 0 %>" data-format="<%= fmt['key'] %>" onclick="selectBandFormat('<%= fmt['key'] %>', <%= fmt['price'] %>, '<%= fmt['name'] %> (<%= fmt['musicians'] %>)', this)">
                  <div style="font-size: 1.4em;"><%= fmt['emoji'] %></div>
                  <strong><%= fmt['name'] %></strong>
                  <span style="font-size: 0.75em; color: var(--text-muted); display: block;"><%= fmt['musicians'] %></span>
                </div>
              <% end %>
            </div>
          </div>

          <%# 2. Cantidad de Shows / Pases en Vivo (Sets de 1 Hora) %>
          <div style="margin-bottom: 25px;">
            <div style="display: flex; justify-content: space-between; align-items: center; margin-bottom: 12px;">
              <label style="font-family: var(--font-outfit); font-weight: 800; font-size: 0.95em; text-transform: uppercase; color: var(--primary); margin: 0;">
                2. Shows / Pases en Vivo (Sets de 60 min)
              </label>
              <span style="font-size: 0.78em; color: var(--text-muted); background: rgba(255,255,255,0.05); padding: 3px 8px; border-radius: 6px;">
                Show en vivo con receso
              </span>
            </div>
            <div style="display: grid; grid-template-columns: repeat(auto-fit, minmax(130px, 1fr)); gap: 10px;">
              <div class="sim-show-pill active" data-shows="1" onclick="selectShows(1, this)" style="background: var(--primary-glow); border: 1px solid var(--primary); border-radius: 12px; padding: 12px; text-align: center; cursor: pointer; transition: all 0.2s;">
                <div style="font-size: 1.2em; margin-bottom: 2px;">⚡</div>
                <strong style="display: block; font-size: 0.92em; color: white;">1 Show</strong>
                <span style="font-size: 0.75em; color: #34d399;">Incluido en Base</span>
              </div>
              <div class="sim-show-pill" data-shows="2" onclick="selectShows(2, this)" style="background: rgba(255,255,255,0.04); border: 1px solid var(--card-border); border-radius: 12px; padding: 12px; text-align: center; cursor: pointer; transition: all 0.2s;">
                <div style="font-size: 1.2em; margin-bottom: 2px;">🔥</div>
                <strong style="display: block; font-size: 0.92em; color: white;">2 Shows</strong>
                <span style="font-size: 0.75em; color: #34d399;">+$<%= @company.landing_calculator_extra_show_price.to_i %> <%= @company.landing_calculator_currency.presence || "USD" %></span>
              </div>
              <div class="sim-show-pill" data-shows="3" onclick="selectShows(3, this)" style="background: rgba(255,255,255,0.04); border: 1px solid var(--card-border); border-radius: 12px; padding: 12px; text-align: center; cursor: pointer; transition: all 0.2s;">
                <div style="font-size: 1.2em; margin-bottom: 2px;">👑</div>
                <strong style="display: block; font-size: 0.92em; color: white;">3 Shows</strong>
                <span style="font-size: 0.75em; color: #34d399;">+$<%= (@company.landing_calculator_extra_show_price.to_i * 2) %> <%= @company.landing_calculator_currency.presence || "USD" %></span>
              </div>
            </div>
          </div>

          <%# 3. Horas de Cobertura de Sonido & DJ Continuo %>
          <div style="margin-bottom: 25px;">
            <div style="display: flex; justify-content: space-between; align-items: center; margin-bottom: 12px;">
              <label style="font-family: var(--font-outfit); font-weight: 800; font-size: 0.95em; text-transform: uppercase; color: var(--primary); margin: 0;">
                3. Cobertura de Sonido &amp; DJ: <span id="sound-hours-display" style="color: white; font-size: 1.1em;"><%= @company.landing_calculator_base_sound_hours %> Horas</span>
              </label>
              <span style="font-size: 0.78em; color: var(--text-muted); background: rgba(255,255,255,0.05); padding: 3px 8px; border-radius: 6px;">
                <%= @company.landing_calculator_base_sound_hours %>h incluidas en base
              </span>
            </div>
            <input type="range" id="sound-hours-slider" min="3" max="8" value="<%= @company.landing_calculator_base_sound_hours %>" step="1" style="width: 100%; accent-color: var(--primary); cursor: pointer;" oninput="updateSoundHours(this.value)">
            <div style="display: flex; justify-content: space-between; font-size: 0.78em; color: var(--text-muted); font-weight: 700; margin-top: 6px;">
              <span>3h</span>
              <span>4h</span>
              <span>5h</span>
              <span><%= @company.landing_calculator_base_sound_hours %>h (Base)</span>
              <span>7h</span>
              <span>8h</span>
            </div>
          </div>

          <%# 4. Efectos y Adicionales Reales de la Empresa %>
          <div style="margin-bottom: 25px;">
            <label style="display: block; font-family: var(--font-outfit); font-weight: 800; font-size: 0.95em; text-transform: uppercase; color: var(--primary); margin-bottom: 12px;">
              4. Añade Efectos Especiales &amp; Producción
            </label>
            <% if @standard_upsells.any? %>
              <div style="display: grid; grid-template-columns: repeat(auto-fit, minmax(160px, 1fr)); gap: 10px;">
                <% @standard_upsells.each do |upsell| %>
                  <div class="sim-addon-card" onclick="toggleAddon(this, '<%= upsell.emoji %> <%= upsell.title %>', <%= upsell.price %>)">
                    <input type="checkbox" style="accent-color: var(--primary);">
                    <div>
                      <strong style="display: block; font-size: 0.9em;"><%= upsell.emoji %> <%= upsell.title %></strong>
                      <span style="font-size: 0.8em; color: #34d399;">+$<%= upsell.price.to_i %> <%= upsell.currency %></span>
                    </div>
                  </div>
                <% end %>
              </div>
            <% else %>
              <p style="color: var(--text-muted); font-size: 0.85em;">No hay adicionales configurados para la landing.</p>
            <% end %>
          </div>

          <%# Fecha y Ubicación Opcionales %>
          <div style="display: grid; grid-template-columns: 1fr 1fr; gap: 12px;">
            <div>
              <label style="display: block; font-size: 0.78em; font-weight: 700; text-transform: uppercase; color: var(--text-muted); margin-bottom: 6px;">Fecha Tentativa</label>
              <input type="date" id="sim-date" class="form-input" style="background: rgba(0,0,0,0.3); color: white; border: 1px solid rgba(255,255,255,0.1); border-radius: 10px; padding: 10px 14px; width: 100%; box-sizing: border-box;" onchange="updateSummary()">
            </div>
            <div>
              <label style="display: block; font-size: 0.78em; font-weight: 700; text-transform: uppercase; color: var(--text-muted); margin-bottom: 6px;">Lugar del Evento</label>
              <input type="text" id="sim-location" placeholder="Ej: Maracaibo, San Francisco, Cabimas..." class="form-input" style="background: rgba(0,0,0,0.3); color: white; border: 1px solid rgba(255,255,255,0.1); border-radius: 10px; padding: 10px 14px; width: 100%; box-sizing: border-box;" oninput="updateSummary()">
            </div>
          </div>
        </div>

        <%# Resumen de Cotización en Vivo %>
        <div style="background: rgba(255, 255, 255, 0.03); border: 1px solid rgba(255, 255, 255, 0.08); border-radius: 20px; padding: 25px; position: sticky; top: 90px;">
          <h3 style="margin: 0 0 15px 0; font-family: var(--font-outfit); font-size: 1.2em; font-weight: 800; color: white;">
            📋 Resumen de tu Presupuesto
          </h3>

          <div style="display: flex; flex-direction: column; gap: 10px; border-bottom: 1px solid rgba(255, 255, 255, 0.08); padding-bottom: 15px; margin-bottom: 15px; font-size: 0.92em;">
            <div style="display: flex; justify-content: space-between;">
              <span style="color: var(--text-muted);" id="sum-format-name"><%= @calculator_formats.first['name'] rescue 'Acústico' %></span>
              <strong id="sum-format-price">$<%= @calculator_formats.first['price'].to_i rescue 400 %> USD</strong>
            </div>
            <div style="display: flex; justify-content: space-between;">
              <span style="color: var(--text-muted);" id="sum-shows-name">1 Show en Vivo (60 min)</span>
              <strong id="sum-shows-price" style="color: #34d399;">Incluido</strong>
            </div>
            <div style="display: flex; justify-content: space-between;">
              <span style="color: var(--text-muted);" id="sum-sound-name"><%= @company.landing_calculator_base_sound_hours %> Horas Sonido &amp; DJ</span>
              <strong id="sum-sound-price" style="color: #34d399;">Incluidas</strong>
            </div>
            <div id="sum-addons-list" style="display: flex; flex-direction: column; gap: 6px;"></div>
          </div>

          <div style="display: flex; justify-content: space-between; align-items: baseline; margin-bottom: 20px;">
            <span style="font-size: 1em; font-weight: 700; color: var(--text-muted); text-transform: uppercase;">Total Estimado:</span>
            <div style="text-align: right;">
              <span style="font-family: var(--font-outfit); font-size: 2.4em; font-weight: 900; color: #34d399;" id="sim-total-price">$<%= @calculator_formats.first['price'].to_i rescue 400 %></span>
              <span style="font-size: 0.9em; color: var(--text-muted);"><%= @company.landing_calculator_currency.presence || "USD" %></span>
            </div>
          </div>

          <a id="btn-whatsapp-quote" href="#" target="_blank" class="btn-cta-primary" style="width: 100%; justify-content: center; text-decoration: none; padding: 14px; background: linear-gradient(135deg, #25d366 0%, #128c7e 100%);">
            <span>💬</span> Reservar por WhatsApp
          </a>
          <p style="text-align: center; font-size: 0.75em; color: var(--text-muted); margin: 10px 0 0 0;">
            Te atenderemos de inmediato para confirmar disponibilidad.
          </p>
        </div>

      </div>
    </div>
  </section>
  <% end %>

  <%# --- 4. GALERÍA AUDIOVISUAL & VIDEOS EN TARIMA --- %>
  <% if @company.landing_section_visible?(:show_media) %>
  <section id="galeria" style="padding: 40px 0 80px;">
    <div style="text-align: center; margin-bottom: 35px;">
      <span class="section-badge">Showcase Audiovisual</span>
      <h2 class="gradient-title" style="font-size: 2.4em; margin: 0;">Instrumentos, Producción &amp; Show en Vivo</h2>
      <p style="color: var(--text-muted); font-size: 1.05em; margin-top: 8px;">Conoce de cerca la calidad de nuestros instrumentos, backline profesional y la energía en tarima.</p>
      
      <%# Filtros de Categoría %>
      <div style="display: flex; gap: 8px; justify-content: center; flex-wrap: wrap; margin-top: 20px;" id="media-filter-buttons">
        <button onclick="filterMediaCategory('all')" class="media-filter-btn active" data-cat="all">Todos</button>
        <button onclick="filterMediaCategory('live_show')" class="media-filter-btn" data-cat="live_show">🔥 En Vivo &amp; Tarima</button>
        <button onclick="filterMediaCategory('instruments')" class="media-filter-btn" data-cat="instruments">🎸 Instrumentos &amp; Backline</button>
        <button onclick="filterMediaCategory('effects')" class="media-filter-btn" data-cat="effects">✨ Efectos &amp; Iluminación</button>
        <button onclick="filterMediaCategory('backstage')" class="media-filter-btn" data-cat="backstage">🎬 Montaje &amp; Backstage</button>
      </div>
    </div>

    <% if @media_items.any? %>
      <div style="display: grid; grid-template-columns: repeat(auto-fill, minmax(280px, 1fr)); gap: 20px;" id="media-grid">
        <% @media_items.each do |item| %>
          <div class="glass-box media-card" data-category="<%= item.category %>" style="display: flex; flex-direction: column; overflow: hidden; padding: 0;">
            <div style="position: relative; width: 100%; aspect-ratio: 16/10; background: #000; overflow: hidden;">
              <% if item.video? && item.media_file.attached? %>
                <video src="<%= url_for(item.media_file) %>" controls preload="metadata" style="width: 100%; height: 100%; object-fit: cover;"></video>
              <% elsif item.youtube? && item.youtube_embed_url.present? %>
                <iframe src="<%= item.youtube_embed_url %>" frameborder="0" allow="accelerometer; autoplay; clipboard-write; encrypted-media; gyroscope; picture-in-picture" allowfullscreen style="width: 100%; height: 100%;"></iframe>
              <% elsif item.image? && item.media_file.attached? %>
                <%= image_tag item.media_file, style: "width: 100%; height: 100%; object-fit: cover;" %>
              <% else %>
                <div style="display: flex; align-items: center; justify-content: center; height: 100%; color: var(--text-muted);">
                  <span>🎬 Video en Vivo</span>
                </div>
              <% end %>
              <span style="position: absolute; top: 12px; left: 12px; background: rgba(0,0,0,0.7); backdrop-filter: blur(8px); color: white; font-size: 0.75em; padding: 4px 10px; border-radius: 6px; font-weight: 700;">
                <%= item.category_label %>
              </span>
            </div>
            <div style="padding: 16px;">
              <h4 style="margin: 0 0 6px 0; font-family: var(--font-outfit); font-size: 1.05em; font-weight: 700; color: white;"><%= item.title %></h4>
              <% if item.description.present? %>
                <p style="margin: 0; font-size: 0.85em; color: var(--text-muted); line-height: 1.4;"><%= item.description %></p>
              <% end %>
            </div>
          </div>
        <% end %>
      </div>
    <% else %>
      <%# Mockup placeholder elegante si no hay videos aún %>
      <div style="display: grid; grid-template-columns: repeat(auto-fit, minmax(280px, 1fr)); gap: 20px;">
        <div class="glass-box" style="padding: 0; overflow: hidden;">
          <div style="background: linear-gradient(135deg, rgba(139, 92, 246, 0.2) 0%, rgba(6, 182, 212, 0.2) 100%); aspect-ratio: 16/10; display: flex; align-items: center; justify-content: center; font-size: 3em;">
            🥁
          </div>
          <div style="padding: 16px;">
            <h4 style="margin: 0 0 4px 0; color: white;">Batería Acústica &amp; Percusión</h4>
            <p style="margin: 0; font-size: 0.85em; color: var(--text-muted);">Sets profesionales con microfonía individual y monitoreo in-ear.</p>
          </div>
        </div>
        <div class="glass-box" style="padding: 0; overflow: hidden;">
          <div style="background: linear-gradient(135deg, rgba(245, 158, 11, 0.2) 0%, rgba(236, 72, 153, 0.2) 100%); aspect-ratio: 16/10; display: flex; align-items: center; justify-content: center; font-size: 3em;">
            🎸
          </div>
          <div style="padding: 16px;">
            <h4 style="margin: 0 0 4px 0; color: white;">Guitarras &amp; Bajos Eléctricos</h4>
            <p style="margin: 0; font-size: 0.85em; color: var(--text-muted);">Modelos Fender y amplificación valvular para sonido impecable.</p>
          </div>
        </div>
        <div class="glass-box" style="padding: 0; overflow: hidden;">
          <div style="background: linear-gradient(135deg, rgba(16, 185, 129, 0.2) 0%, rgba(59, 130, 246, 0.2) 100%); aspect-ratio: 16/10; display: flex; align-items: center; justify-content: center; font-size: 3em;">
            ✨
          </div>
          <div style="padding: 16px;">
            <h4 style="margin: 0 0 4px 0; color: white;">Pirotecnia Fría &amp; Geisers</h4>
            <p style="margin: 0; font-size: 0.85em; color: var(--text-muted);">Efectos especiales homologados para interiores y momentos estelares.</p>
          </div>
        </div>
      </div>
    <% end %>
  </section>
  <% end %>

  <%# --- 5. EQUIPO DE MÚSICOS & TALENTO --- %>
  <% if @company.landing_section_visible?(:show_team) && @staff_members.any? %>
    <section id="equipo" style="padding: 40px 0 70px;">
      <div style="text-align: center; margin-bottom: 35px;">
        <span class="section-badge">Músicos &amp; Producción</span>
        <h2 class="gradient-title" style="font-size: 2.3em; margin: 0;">Conoce a los Artistas en Tarima</h2>
      </div>

      <div style="display: grid; grid-template-columns: repeat(auto-fit, minmax(220px, 1fr)); gap: 20px;">
        <% @staff_members.each do |staff| %>
          <div class="glass-box" style="text-align: center; padding: 24px;">
            <div style="width: 95px; height: 95px; border-radius: 50%; margin: 0 auto 14px; overflow: hidden; border: 3px solid var(--card-border-hover); box-shadow: 0 0 20px var(--primary-glow); background: rgba(255,255,255,0.05); display: flex; align-items: center; justify-content: center;">
              <% if staff.avatar_attached? %>
                <%= image_tag staff.avatar_url_or_data, style: "width: 100%; height: 100%; object-fit: cover;" %>
              <% else %>
                <div style="width: 100%; height: 100%; background: linear-gradient(135deg, var(--primary) 0%, var(--accent-cyan) 100%); display: flex; align-items: center; justify-content: center; font-family: var(--font-outfit); font-size: 2em; font-weight: 800; color: white;">
                  <%= staff.display_name[0].upcase %>
                </div>
              <% end %>
            </div>
            <h4 style="margin: 0 0 4px 0; font-family: var(--font-outfit); font-size: 1.1em; font-weight: 700; color: white;"><%= staff.display_name %></h4>
            <% if staff.specialty.present? %>
              <span style="display: inline-block; background: var(--primary-glow); color: var(--primary); padding: 3px 10px; border-radius: 99px; font-size: 0.75em; font-weight: 800; text-transform: uppercase;">
                <%= staff.specialty %>
              </span>
            <% end %>
            <% if staff.bio.present? %>
              <p style="margin: 10px 0 0 0; font-size: 0.85em; color: var(--text-muted); line-height: 1.4;">
                <%= staff.bio %>
              </p>
            <% end %>
          </div>
        <% end %>
      </div>
    </section>
  <% end %>

  <%# --- 6. TESTIMONIOS & RESEÑAS 5 ESTRELLAS --- %>
  <% if @company.landing_section_visible?(:show_reviews) %>
  <section id="resenas" style="padding: 40px 0 80px;">
    <div style="text-align: center; margin-bottom: 35px;">
      <span class="section-badge">Opiniones Reales</span>
      <h2 class="gradient-title" style="font-size: 2.3em; margin: 0;">Lo que Dicen Quienes ya Celebraron con Nosotros</h2>
    </div>

    <div style="display: grid; grid-template-columns: repeat(auto-fit, minmax(280px, 1fr)); gap: 20px;">
      <% if @reviews.any? %>
        <% @reviews.each do |review| %>
          <div class="glass-box" style="padding: 24px;">
            <div style="display: flex; justify-content: space-between; margin-bottom: 10px;">
              <strong style="color: white; font-size: 1.05em;"><%= review.client_name %></strong>
              <span style="color: #fbbf24;"><%= review.stars_display %></span>
            </div>
            <p style="margin: 0; color: #cbd5e1; font-size: 0.92em; line-height: 1.5; font-style: italic;">
              "<%= review.comment %>"
            </p>
          </div>
        <% end %>
      <% else %>
        <div class="glass-box" style="padding: 24px;">
          <div style="display: flex; justify-content: space-between; margin-bottom: 10px;">
            <strong style="color: white; font-size: 1.05em;">María &amp; Carlos (Boda)</strong>
            <span style="color: #fbbf24;">★★★★★</span>
          </div>
          <p style="margin: 0; color: #cbd5e1; font-size: 0.92em; line-height: 1.5; font-style: italic;">
            "¡Increíble la energía de la banda! Todos nuestros invitados bailaron hasta el final. La puntualidad y el sonido fueron perfectos."
          </p>
        </div>
        <div class="glass-box" style="padding: 24px;">
          <div style="display: flex; justify-content: space-between; margin-bottom: 10px;">
            <strong style="color: white; font-size: 1.05em;">Alejandro R. (Evento Corporativo)</strong>
            <span style="color: #fbbf24;">★★★★★</span>
          </div>
          <p style="margin: 0; color: #cbd5e1; font-size: 0.92em; line-height: 1.5; font-style: italic;">
            "Superaron todas las expectativas. El repertorio fue muy versátil y la calidad del audio impecable."
          </p>
        </div>
        <div class="glass-box" style="padding: 24px;">
          <div style="display: flex; justify-content: space-between; margin-bottom: 10px;">
            <strong style="color: white; font-size: 1.05em;">Valentina G. (Cumpleaños 30)</strong>
            <span style="color: #fbbf24;">★★★★★</span>
          </div>
          <p style="margin: 0; color: #cbd5e1; font-size: 0.92em; line-height: 1.5; font-style: italic;">
            "Recomendados al 100%. Hicieron de mi fiesta una noche inolvidable. ¡Gracias por tanta buena vibra!"
          </p>
        </div>
      <% end %>
    </div>
  </section>
  <% end %>

  <%# --- 7. FAQ INTERACTIVO (PREGUNTAS FRECUENTES) --- %>
  <% if @company.landing_section_visible?(:show_faq) %>
  <section id="faq" style="padding: 40px 0 80px;">
    <div style="text-align: center; margin-bottom: 35px;">
      <span class="section-badge">Dudas &amp; Respuestas</span>
      <h2 class="gradient-title" style="font-size: 2.3em; margin: 0;">Preguntas Frecuentes</h2>
    </div>

    <div style="max-width: 800px; margin: 0 auto; display: flex; flex-direction: column; gap: 14px;">
      <% @faqs.each_with_index do |faq, idx| %>
        <div class="glass-box" style="padding: 18px 24px; cursor: pointer;" onclick="toggleFaqAccordion(<%= idx %>)">
          <div style="display: flex; justify-content: space-between; align-items: center;">
            <h4 style="margin: 0; font-family: var(--font-outfit); font-size: 1.08em; font-weight: 700; color: white;">
              <%= faq['q'] %>
            </h4>
            <span id="faq-arrow-<%= idx %>" style="color: var(--primary); font-size: 1.2em; font-weight: bold;">▼</span>
          </div>
          <div id="faq-ans-<%= idx %>" style="display: <%= idx == 0 ? 'block' : 'none' %>; margin-top: 12px; color: var(--text-muted); font-size: 0.92em; line-height: 1.5; border-top: 1px solid rgba(255,255,255,0.06); padding-top: 10px;">
            <%= faq['a'] %>
          </div>
        </div>
      <% end %>
    </div>
  </section>
  <% end %>

  <%# --- 8. FOOTER / CONTACTO FINAL --- %>
  <footer style="border-top: 1px solid rgba(255, 255, 255, 0.08); padding: 50px 0 30px; text-align: center; margin-top: 40px;">
    <h3 style="font-family: var(--font-outfit); font-size: 1.6em; font-weight: 800; color: white; margin: 0 0 10px 0;">
      <%= @company.name %>
    </h3>
    <p style="color: var(--text-muted); font-size: 0.95em; max-width: 500px; margin: 0 auto 20px;">
      <%= @company.tagline.presence || "Música en vivo de alto impacto para bodas, celebraciones y eventos de gala." %>
    </p>

    <div style="display: flex; justify-content: center; gap: 15px; margin-bottom: 25px;">
      <% if @company.instagram_url.present? %>
        <a href="<%= @company.instagram_url %>" target="_blank" style="color: white; text-decoration: none; font-size: 1.2em; background: rgba(255,255,255,0.06); padding: 8px 14px; border-radius: 10px;">Instagram ↗</a>
      <% end %>
      <% if @company.youtube_url.present? %>
        <a href="<%= @company.youtube_url %>" target="_blank" style="color: white; text-decoration: none; font-size: 1.2em; background: rgba(255,255,255,0.06); padding: 8px 14px; border-radius: 10px;">YouTube ↗</a>
      <% end %>
      <% if @company.tiktok_url.present? %>
        <a href="<%= @company.tiktok_url %>" target="_blank" style="color: white; text-decoration: none; font-size: 1.2em; background: rgba(255,255,255,0.06); padding: 8px 14px; border-radius: 10px;">TikTok ↗</a>
      <% end %>
    </div>

    <p style="color: rgba(255,255,255,0.3); font-size: 0.8em; margin: 0;">
      © <%= Time.current.year %> <%= @company.name %>. Todos los derechos reservados. Impulsado por GigManager.
    </p>
  </footer>

</div>

<%# --- ESTILOS CSS DE INTERFAZ & COMPONENTES --- %>
<style>
  .section-badge {
    display: inline-block;
    background: var(--primary-glow);
    color: var(--primary);
    padding: 6px 14px;
    border-radius: 99px;
    font-size: 0.82em;
    font-weight: 800;
    text-transform: uppercase;
    letter-spacing: 0.5px;
    margin-bottom: 12px;
    border: 1px solid var(--primary);
  }

  .gradient-title {
    font-family: var(--font-heading);
    font-weight: 900;
    background: var(--primary-gradient);
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    letter-spacing: -0.5px;
  }

  .btn-cta-primary {
    background: var(--primary-gradient);
    color: white;
    font-family: var(--font-heading);
    font-weight: 800;
    font-size: 1.05em;
    padding: 14px 28px;
    border-radius: 14px;
    border: none;
    cursor: pointer;
    box-shadow: 0 4px 20px var(--primary-glow);
    display: inline-flex;
    align-items: center;
    gap: 10px;
    transition: transform 0.2s, box-shadow 0.2s;
  }
  .btn-cta-primary:hover {
    transform: translateY(-2px);
    box-shadow: 0 8px 30px var(--primary-glow);
  }

  /* ==========================================
     PLANTILLA 1: CLÁSICA ESCENARIO (classic_stage)
     ========================================== */
  .template-classic-stage .glass-box {
    border-radius: 20px;
    background: rgba(15, 23, 42, 0.65);
    border: 1px solid rgba(255, 255, 255, 0.08);
    backdrop-filter: blur(16px);
  }

  /* ==========================================
     PLANTILLA 2: NEÓN FESTIVAL CYBERPUNK (neon_festival)
     ========================================== */
  .template-neon-festival {
    background-image: 
      linear-gradient(rgba(139, 92, 246, 0.06) 1px, transparent 1px),
      linear-gradient(90deg, rgba(139, 92, 246, 0.06) 1px, transparent 1px) !important;
    background-size: 36px 36px !important;
  }
  .template-neon-festival .glass-box {
    border: 2px solid var(--primary) !important;
    box-shadow: 0 0 25px var(--primary-glow), inset 0 0 15px var(--primary-glow) !important;
    border-radius: 8px !important;
    background: rgba(5, 7, 15, 0.95) !important;
  }
  .template-neon-festival .section-badge {
    border: 2px solid var(--primary) !important;
    border-radius: 4px !important;
    font-family: 'Space Grotesk', sans-serif !important;
    letter-spacing: 2px !important;
    box-shadow: 0 0 15px var(--primary-glow) !important;
    background: #000000 !important;
    color: #ffffff !important;
  }
  .template-neon-festival .gradient-title {
    font-family: 'Space Grotesk', sans-serif !important;
    text-transform: uppercase !important;
    letter-spacing: 1px !important;
    text-shadow: 0 0 20px var(--primary-glow);
  }
  .template-neon-festival .btn-cta-primary {
    border-radius: 6px !important;
    border: 2px solid #ffffff !important;
    box-shadow: 0 0 30px var(--primary) !important;
    font-family: 'Space Grotesk', sans-serif !important;
    text-transform: uppercase !important;
    letter-spacing: 1px !important;
  }
  .template-neon-festival .sim-pill {
    border-radius: 6px !important;
    border: 1px solid var(--primary) !important;
    font-family: 'Space Grotesk', sans-serif !important;
  }

  /* ==========================================
     PLANTILLA 3: ROYAL GALA LUXURY & BODAS VIP (royal_gala)
     ========================================== */
  .template-royal-gala {
    background-color: #040711 !important;
    background-image: 
      radial-gradient(circle at 50% 15%, rgba(234, 179, 8, 0.15) 0%, transparent 60%),
      radial-gradient(circle at 85% 65%, rgba(202, 138, 4, 0.1) 0%, transparent 50%) !important;
  }
  .template-royal-gala .glass-box {
    border: 1px solid rgba(234, 179, 8, 0.35) !important;
    border-radius: 24px !important;
    background: rgba(12, 16, 28, 0.85) !important;
    box-shadow: 0 15px 40px rgba(0,0,0,0.6) !important;
  }
  .template-royal-gala .section-badge {
    border: 1px solid #eab308 !important;
    color: #fef08a !important;
    border-radius: 99px !important;
    background: rgba(234, 179, 8, 0.12) !important;
    font-family: 'Playfair Display', serif !important;
    font-style: italic !important;
    letter-spacing: 1px !important;
  }
  .template-royal-gala .gradient-title {
    font-family: 'Playfair Display', serif !important;
    font-style: italic !important;
    background: linear-gradient(135deg, #fef08a 0%, #eab308 50%, #ca8a04 100%) !important;
    -webkit-background-clip: text !important;
    -webkit-text-fill-color: transparent !important;
  }
  .template-royal-gala .btn-cta-primary {
    background: linear-gradient(135deg, #eab308 0%, #ca8a04 100%) !important;
    color: #0f172a !important;
    font-family: 'Playfair Display', serif !important;
    font-weight: 900 !important;
    border-radius: 30px !important;
    box-shadow: 0 8px 30px rgba(234, 179, 8, 0.4) !important;
  }
  .template-royal-gala .sim-pill {
    border-radius: 20px !important;
    border: 1px solid rgba(234, 179, 8, 0.3) !important;
  }
  .template-royal-gala .sim-pill.active {
    background: rgba(234, 179, 8, 0.2) !important;
    border-color: #eab308 !important;
    box-shadow: 0 0 20px rgba(234, 179, 8, 0.3) !important;
  }

  /* ==========================================
     PLANTILLA 4: MINIMAL STUDIO & ACÚSTICO (minimal_acoustic)
     ========================================== */
  .template-minimal-acoustic {
    background-color: #08090c !important;
    background-image: none !important;
  }
  .template-minimal-acoustic .glass-box {
    background: rgba(15, 17, 23, 0.95) !important;
    border: 1px solid rgba(255, 255, 255, 0.12) !important;
    border-radius: 12px !important;
    box-shadow: 0 4px 15px rgba(0,0,0,0.4) !important;
  }
  .template-minimal-acoustic .section-badge {
    background: rgba(255, 255, 255, 0.08) !important;
    color: #f8fafc !important;
    border: 1px solid rgba(255, 255, 255, 0.15) !important;
    border-radius: 6px !important;
    font-family: 'Inter', sans-serif !important;
    letter-spacing: 1px !important;
  }
  .template-minimal-acoustic .gradient-title {
    font-family: 'Inter', sans-serif !important;
    font-weight: 800 !important;
    color: #ffffff !important;
    background: none !important;
    -webkit-text-fill-color: initial !important;
  }
  .template-minimal-acoustic .btn-cta-primary {
    background: #ffffff !important;
    color: #000000 !important;
    border-radius: 8px !important;
    font-family: 'Inter', sans-serif !important;
    font-weight: 800 !important;
    box-shadow: 0 4px 15px rgba(255, 255, 255, 0.2) !important;
  }
  .template-minimal-acoustic .sim-pill {
    border-radius: 8px !important;
    border: 1px solid rgba(255, 255, 255, 0.15) !important;
  }

  .btn-cta-secondary {
    background: rgba(255, 255, 255, 0.06);
    color: white;
    font-family: var(--font-outfit);
    font-weight: 700;
    font-size: 1em;
    padding: 14px 24px;
    border-radius: 14px;
    border: 1px solid rgba(255, 255, 255, 0.15);
    text-decoration: none;
    display: inline-flex;
    align-items: center;
    gap: 8px;
    transition: background 0.2s;
  }
  .btn-cta-secondary:hover {
    background: rgba(255, 255, 255, 0.12);
  }

  /* Píldoras del Simulador */
  .sim-pill {
    background: rgba(255, 255, 255, 0.04);
    border: 1px solid var(--card-border);
    border-radius: 12px;
    padding: 14px 10px;
    text-align: center;
    cursor: pointer;
    transition: all 0.2s ease;
  }
  .sim-pill:hover {
    border-color: var(--primary);
    background: rgba(255, 255, 255, 0.07);
  }
  .sim-pill.active {
    background: var(--primary-glow);
    border-color: var(--primary);
    box-shadow: 0 0 16px var(--primary-glow);
  }

  /* Tarjetas de Adicionales */
  .sim-addon-card {
    background: rgba(255, 255, 255, 0.04);
    border: 1px solid var(--card-border);
    border-radius: 12px;
    padding: 12px 14px;
    display: flex;
    align-items: center;
    gap: 12px;
    cursor: pointer;
    transition: all 0.2s ease;
  }
  .sim-addon-card:hover {
    border-color: var(--primary);
  }
  .sim-addon-card.active {
    background: var(--primary-glow);
    border-color: var(--primary);
  }

  /* Botones de Filtro Multimedia */
  .media-filter-btn {
    background: rgba(255, 255, 255, 0.05);
    border: 1px solid rgba(255, 255, 255, 0.1);
    color: var(--text-muted);
    padding: 8px 16px;
    border-radius: 99px;
    font-size: 0.85em;
    font-weight: 700;
    cursor: pointer;
    transition: all 0.2s;
  }
  .media-filter-btn:hover {
    background: rgba(255, 255, 255, 0.1);
    color: white;
  }
  .media-filter-btn.active {
    background: linear-gradient(135deg, var(--primary) 0%, var(--primary-hover) 100%);
    border-color: var(--primary);
    color: white;
    box-shadow: 0 2px 10px var(--primary-glow);
  }
</style>

<%# --- JAVASCRIPT DEL SIMULADOR EN VIVO (LÓGICA SEPARADA: SHOWS VS SONIDO) & GALERÍA --- %>
<script>
  const baseShowsCount = <%= @company.landing_calculator_base_shows || 1 %>;
  const extraShowPrice = <%= @company.landing_calculator_extra_show_price || 250.0 %>;
  const baseSoundHours = <%= @company.landing_calculator_base_sound_hours || 4 %>;
  const extraSoundHourRate = <%= @company.landing_calculator_extra_sound_hour_price || 60.0 %>;
  const defaultCurrency = '<%= @company.landing_calculator_currency.presence || "USD" %>';

  <% first_fmt = @calculator_formats.first || { 'price' => 400, 'name' => 'Acústico', 'musicians' => 'Dúo' } %>
  let baseFormatPrice = <%= first_fmt['price'].to_f %>;
  let baseFormatName = "<%= first_fmt['name'] %> (<%= first_fmt['musicians'] %>)";
  let selectedShows = 1;
  let selectedSoundHours = baseSoundHours;
  let activeAddons = [];

  function selectBandFormat(key, price, name, element) {
    document.querySelectorAll('.sim-pill').forEach(el => el.classList.remove('active'));
    element.classList.add('active');
    baseFormatPrice = price;
    baseFormatName = name;
    updateSummary();
  }

  function selectShows(showsCount, element) {
    document.querySelectorAll('.sim-show-pill').forEach(el => {
      el.classList.remove('active');
      el.style.borderColor = 'var(--card-border)';
      el.style.background = 'rgba(255,255,255,0.04)';
    });
    element.classList.add('active');
    element.style.borderColor = 'var(--primary)';
    element.style.background = 'var(--primary-glow)';
    selectedShows = parseInt(showsCount);
    updateSummary();
  }

  function updateSoundHours(hours) {
    selectedSoundHours = parseInt(hours);
    document.getElementById('sound-hours-display').textContent = `${hours} Horas`;
    updateSummary();
  }

  function selectAndScrollToFormat(key, price, name) {
    const pill = document.querySelector(`.sim-pill[data-format="${key}"]`);
    if (pill) {
      selectBandFormat(key, price, name, pill);
    }
    const cotizadorSection = document.getElementById('cotizador');
    if (cotizadorSection) {
      cotizadorSection.scrollIntoView({ behavior: 'smooth' });
    }
  }

  function toggleAddon(element, addonName, addonPrice) {
    const checkbox = element.querySelector('input[type="checkbox"]');
    checkbox.checked = !checkbox.checked;
    
    if (checkbox.checked) {
      element.classList.add('active');
      activeAddons.push({ name: addonName, price: addonPrice });
    } else {
      element.classList.remove('active');
      activeAddons = activeAddons.filter(a => a.name !== addonName);
    }
    updateSummary();
  }

  function updateSummary() {
    document.getElementById('sum-format-name').textContent = baseFormatName;
    document.getElementById('sum-format-price').textContent = `$${baseFormatPrice} ${defaultCurrency}`;

    // 1. Shows en Vivo (1 Hora por Pase)
    let extraShowsCost = 0;
    if (selectedShows > 1) {
      extraShowsCost = (selectedShows - 1) * extraShowPrice;
      document.getElementById('sum-shows-name').textContent = `${selectedShows} Shows en Vivo (${selectedShows} Pases de 60m)`;
      document.getElementById('sum-shows-price').textContent = `+$${Math.round(extraShowsCost)} ${defaultCurrency}`;
      document.getElementById('sum-shows-price').style.color = '#34d399';
    } else {
      document.getElementById('sum-shows-name').textContent = "1 Show en Vivo (Pase de 60m)";
      document.getElementById('sum-shows-price').textContent = "Incluido";
      document.getElementById('sum-shows-price').style.color = '#34d399';
    }

    // 2. Cobertura de Sonido & DJ
    let extraSoundCost = 0;
    if (selectedSoundHours > baseSoundHours) {
      extraSoundCost = (selectedSoundHours - baseSoundHours) * extraSoundHourRate;
      document.getElementById('sum-sound-name').textContent = `${selectedSoundHours} Horas Sonido (+${selectedSoundHours - baseSoundHours}h extra)`;
      document.getElementById('sum-sound-price').textContent = `+$${Math.round(extraSoundCost)} ${defaultCurrency}`;
      document.getElementById('sum-sound-price').style.color = '#34d399';
    } else if (selectedSoundHours < baseSoundHours) {
      extraSoundCost = (selectedSoundHours - baseSoundHours) * (extraSoundHourRate * 0.5);
      document.getElementById('sum-sound-name').textContent = `${selectedSoundHours} Horas Sonido & DJ`;
      document.getElementById('sum-sound-price').textContent = `-$${Math.abs(Math.round(extraSoundCost))} ${defaultCurrency}`;
      document.getElementById('sum-sound-price').style.color = '#cbd5e1';
    } else {
      document.getElementById('sum-sound-name').textContent = `${selectedSoundHours} Horas Sonido & DJ`;
      document.getElementById('sum-sound-price').textContent = "Incluidas";
      document.getElementById('sum-sound-price').style.color = '#34d399';
    }

    // 3. Adicionales
    const addonsContainer = document.getElementById('sum-addons-list');
    addonsContainer.innerHTML = '';
    let addonsTotal = 0;

    activeAddons.forEach(addon => {
      addonsTotal += addon.price;
      const row = document.createElement('div');
      row.style.display = 'flex';
      row.style.justifyContent = 'space-between';
      row.innerHTML = `<span style="color: var(--text-muted);">${addon.name}</span><strong>+$${addon.price} ${defaultCurrency}</strong>`;
      addonsContainer.appendChild(row);
    });

    const total = baseFormatPrice + extraShowsCost + extraSoundCost + addonsTotal;
    document.getElementById('sim-total-price').textContent = `$${Math.round(total)}`;

    // 4. Construir mensaje de WhatsApp
    const dateVal = document.getElementById('sim-date').value || "Por definir";
    const locVal = document.getElementById('sim-location').value || "Por confirmar";
    
    let addonsText = activeAddons.length > 0 ? activeAddons.map(a => a.name).join(", ") : "Ninguno";
    
    let waMsg = `¡Hola <%= @company.name %>! Estuve calculando en su cotizador web y me gustaría reservar:\n` +
      `• Formato: ${baseFormatName}\n` +
      `• Shows en Vivo: ${selectedShows} ${selectedShows === 1 ? 'Show (1 Pase de 60m)' : 'Shows (' + selectedShows + ' Pases de 60m con receso)'}\n` +
      `• Cobertura de Sonido & DJ: ${selectedSoundHours} Horas\n` +
      `• Efectos/Adicionales: ${addonsText}\n` +
      `• Fecha tentativa: ${dateVal}\n` +
      `• Lugar del Evento: ${locVal}\n` +
      `• Presupuesto estimado: $${Math.round(total)} ${defaultCurrency}\n\n` +
      `¿Tienen disponibilidad para esta fecha?`;

    const waNumber = '<%= @whatsapp_number.gsub(/[^0-9]/, "") %>';
    const waBtn = document.getElementById('btn-whatsapp-quote');
    waBtn.href = `https://wa.me/${waNumber}?text=${encodeURIComponent(waMsg)}`;
  }

  // Filtrado de Galería Audiovisual
  function filterMediaCategory(category) {
    document.querySelectorAll('.media-filter-btn').forEach(btn => btn.classList.remove('active'));
    document.querySelector(`.media-filter-btn[data-cat="${category}"]`)?.classList.add('active');

    const cards = document.querySelectorAll('.media-card');
    cards.forEach(card => {
      if (category === 'all' || card.getAttribute('data-category') === category) {
        card.style.display = 'flex';
      } else {
        card.style.display = 'none';
      }
    });
  }

  // Acordeón de FAQs
  function toggleFaqAccordion(idx) {
    const ans = document.getElementById(`faq-ans-${idx}`);
    const arrow = document.getElementById(`faq-arrow-${idx}`);
    if (!ans) return;
    if (ans.style.display === 'block') {
      ans.style.display = 'none';
      if (arrow) arrow.textContent = '▼';
    } else {
      ans.style.display = 'block';
      if (arrow) arrow.textContent = '▲';
    }
  }

  // Inicializar cálculo
  updateSummary();
</script>
