<!DOCTYPE html>
<html>
  <head>
    <title>GigManager</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <!-- PWA & Mobile Web App Meta Tags -->
    <meta name="theme-color" content="#0f172a">
    <meta name="mobile-web-app-capable" content="yes">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="apple-mobile-web-app-status-bar-style" content="black-translucent">
    <meta name="apple-mobile-web-app-title" content="GigManager">
    <link rel="manifest" href="/manifest.json">
    <link rel="apple-touch-icon" href="/icons/apple-touch-icon.png">
    <link rel="apple-touch-icon" sizes="180x180" href="/icons/apple-touch-icon.png">
    <link rel="icon" type="image/png" sizes="192x192" href="/icons/icon-192.png">
    <link rel="icon" type="image/png" sizes="512x512" href="/icons/icon-512.png">

    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;600;700;800&display=swap" rel="stylesheet">
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
    <style>
      /* Hide native scrollbar but keep scroll functionality */
      aside {
        /* allow flex children to shrink/overflow correctly */
        min-height: 0;
      }
      aside nav {
        scrollbar-width: none; /* Firefox */
        -ms-overflow-style: none; /* IE 10+ */
        position: relative; /* container for custom indicator */
        /* ensure nav can scroll inside the flex layout */
        min-height: 0;
        overflow-y: auto;
        -webkit-overflow-scrolling: touch;
      }
      aside nav::-webkit-scrollbar { display: none; }

      /* Custom scroll indicator (a subtle background bar acting as thumb) */
      #sidebar-scroll-indicator {
        position: absolute;
        right: 6px;
        top: 0;
        width: 6px;
        background: rgba(255,255,255,0.08);
        border-radius: 6px;
        transition: transform 0.12s linear, height 0.12s linear, opacity 0.12s linear;
        will-change: transform, height;
        opacity: 0.9;
      }

      /* Prevent long labels from wrapping awkwardly */
      aside nav a { white-space: nowrap; overflow: hidden; text-overflow: ellipsis; }

      /* ======================================================
         MOBILE TOPBAR — oculto en desktop, visible en móvil
         ====================================================== */
      #mobile-topbar {
        display: none;
      }

      /* ======================================================
         MOBILE BACKDROP — oculto siempre hasta activación JS
         ====================================================== */
      #mobile-backdrop {
        display: none;
        position: fixed;
        inset: 0;
        background: rgba(10, 18, 35, 0.6);
        backdrop-filter: blur(3px);
        -webkit-backdrop-filter: blur(3px);
        z-index: 998;
      }
      #mobile-backdrop.open { display: block; }

      /* ======================================================
         BOTÓN CERRAR dentro del sidebar — oculto en desktop
         ====================================================== */
      #sidebar-close-btn {
        display: none;
      }

      /* ======================================================
         RESPONSIVE: todo el cambio ocurre SOLO aquí (≤ 991px)
         ====================================================== */
      @media (max-width: 991px) {

        /* Topbar visible */
        #mobile-topbar {
          display: flex;
          align-items: center;
          justify-content: space-between;
          position: fixed;
          top: 0; left: 0; right: 0;
          height: 58px;
          background: #0f172a;
          z-index: 1000;
          padding: 0 16px;
          box-shadow: 0 2px 12px rgba(0,0,0,0.25);
          border-bottom: 1px solid rgba(255,255,255,0.07);
        }

        #mobile-topbar .topbar-brand {
          display: flex;
          align-items: center;
          gap: 10px;
          color: white;
          font-family: 'Inter', sans-serif;
          font-weight: 800;
          font-size: 1.05rem;
        }
        #mobile-topbar .topbar-logo {
          background: linear-gradient(135deg, #2563eb, #3b82f6);
          color: white;
          width: 30px; height: 30px;
          display: flex; align-items: center; justify-content: center;
          border-radius: 8px;
          font-size: 0.85rem;
          font-weight: 800;
          flex-shrink: 0;
        }

        #mobile-topbar .topbar-hamburger {
          background: rgba(255,255,255,0.08);
          border: 1px solid rgba(255,255,255,0.12);
          border-radius: 9px;
          width: 40px; height: 40px;
          display: flex; flex-direction: column;
          align-items: center; justify-content: center;
          gap: 5px;
          cursor: pointer;
          flex-shrink: 0;
        }
        #mobile-topbar .topbar-hamburger span {
          display: block;
          width: 20px; height: 2px;
          background: white;
          border-radius: 2px;
        }

        /* Sidebar se convierte en drawer off-canvas */
        .app-sidebar {
          position: fixed !important;
          top: 0; left: 0; bottom: 0;
          width: 280px !important;
          max-width: 82vw;
          transform: translateX(-100%);
          transition: transform 0.28s cubic-bezier(0.4, 0, 0.2, 1);
          z-index: 999;
          overflow-y: auto;
        }
        .app-sidebar.sidebar-open {
          transform: translateX(0);
        }

        /* Botón cerrar sidebar visible */
        #sidebar-close-btn {
          display: flex;
          align-items: center; justify-content: center;
          position: absolute;
          top: 14px; right: 14px;
          width: 32px; height: 32px;
          background: rgba(255,255,255,0.08);
          border: 1px solid rgba(255,255,255,0.12);
          border-radius: 8px;
          color: #94a3b8;
          font-size: 1.3rem;
          line-height: 1;
          cursor: pointer;
          z-index: 10;
          text-decoration: none;
        }

        /* Main: quitar margin-left que aplica inline en desktop y poner top padding para topbar */
        main {
          margin-left: 0 !important;
          padding-top: calc(58px + 20px) !important;
          padding-left: 14px !important;
          padding-right: 14px !important;
          min-width: 0;
          overflow-x: hidden;
        }

        /* Tablas: scroll horizontal en móvil sin romper layout */
        table {
          min-width: 500px;
        }
        .table-custom-wrapper,
        div:has(> table) {
          overflow-x: auto;
          -webkit-overflow-scrolling: touch;
        }

        /* Inputs/selects: fuente ≥16px para evitar zoom automático en iOS */
        input, select, textarea {
          font-size: 16px !important;
        }

        /* Ajustes de responsividad para teléfonos móviles (estrechos) */
        .notifications-container {
          padding-left: 2px !important;
          padding-right: 2px !important;
        }

        .notifications-header {
          padding: 18px 16px !important;
          border-radius: 14px !important;
        }

        .mobile-full-width,
        .mobile-full-width form,
        .mobile-full-width button {
          width: 100% !important;
        }

        .notification-card {
          padding: 16px 14px !important;
          border-radius: 12px !important;
        }

        .notification-card > div {
          flex-direction: column !important;
          align-items: stretch !important;
        }

        .btn-action-container {
          width: 100% !important;
          display: flex !important;
          flex-direction: column !important;
          align-items: stretch !important;
          margin-top: 10px !important;
        }

        .btn-action-container a,
        .btn-action-container form,
        .btn-action-container button {
          width: 100% !important;
          text-align: center !important;
          justify-content: center !important;
          box-sizing: border-box !important;
        }

        .nav-tabs-scroll {
          padding: 4px !important;
          border-radius: 12px !important;
          overflow-x: auto !important;
          -webkit-overflow-scrolling: touch !important;
          scrollbar-width: none !important;
        }
        .nav-tabs-scroll::-webkit-scrollbar {
          display: none !important;
        }
      }

      @keyframes toastProgress {
        from { transform: scaleX(1); }
        to   { transform: scaleX(0); }
      }
    </style>
  </head>

  <body style="margin: 0; padding: 0; background-color: #f8fafc;">

    <%# =============================================
        TOPBAR MÓVIL (oculto en desktop por CSS)
        ============================================= %>
    <% if user_signed_in? %>
      <header id="mobile-topbar" aria-label="Barra de navegación móvil">
        <button class="topbar-hamburger" id="hamburger-btn" aria-label="Abrir menú" aria-expanded="false" aria-controls="app-sidebar">
          <span></span><span></span><span></span>
        </button>
        <div class="topbar-brand">
          <span class="topbar-logo">G</span>
          Gig Manager
        </div>
        <div style="display: flex; align-items: center; gap: 8px;">
          <%= link_to notifications_path, style: "background: rgba(255,255,255,0.08); border: 1px solid rgba(255,255,255,0.12); color: white; width: 36px; height: 36px; border-radius: 10px; display: flex; align-items: center; justify-content: center; position: relative; text-decoration: none; font-size: 1.05rem;", aria: { label: "Notificaciones" } do %>
            🔔
            <% unread_count = current_user&.unread_notifications_count.to_i %>
            <span id="mobile-topbar-unread-badge" style="<%= unread_count > 0 ? '' : 'display: none;' %> position: absolute; top: -4px; right: -4px; background: #ef4444; color: white; border-radius: 99px; font-size: 0.65rem; font-weight: 800; min-width: 17px; height: 17px; line-height: 17px; text-align: center; padding: 0 3px; border: 2px solid #0f172a;">
              <%= unread_count %>
            </span>
          <% end %>
        </div>
      </header>

      <%# Backdrop para cerrar sidebar en móvil %>
      <div id="mobile-backdrop" aria-hidden="true"></div>
    <% end %>

    <% if user_signed_in? && current_company.present? %>
      <%= turbo_stream_from current_company, "notifications_#{current_user.role}" %>
      <%= turbo_stream_from current_company, "notifications_all_areas" %>
    <% end %>

    <div style="display: flex; min-height: 100vh; font-family: 'Inter', sans-serif;">
      
      <%# --- SIDEBAR --- %>
      <% if user_signed_in? %>
        <aside class="app-sidebar" id="app-sidebar">
          
          <%# Logo + nombre de empresa + botón cerrar (sólo visible en móvil) %>
          <div class="sidebar-brand" style="position: relative;">
            <h2>
              <span class="brand-logo">G</span>
              Gig Manager
            </h2>
            <% if current_company.present? %>
              <div style="font-size: 0.78rem; color: #818cf8; font-weight: 600; margin-top: 4px; display: flex; align-items: center; gap: 4px;">
                <span>🏢</span> <span><%= current_company.name %></span>
              </div>
            <% end %>
            <button id="sidebar-close-btn" aria-label="Cerrar menú">&times;</button>
          </div>

          <%# Navegación %>
          <nav class="sidebar-nav">
            <% if current_user.superadmin? %>
              <div style="background: rgba(99, 102, 241, 0.15); border: 1px solid rgba(99, 102, 241, 0.3); border-radius: 12px; padding: 10px; margin-bottom: 12px;">
                <div style="font-size: 0.75rem; font-weight: 800; color: #a5b4fc; text-transform: uppercase; letter-spacing: 0.05em; margin-bottom: 6px;">👑 Superadmin</div>
                <%= link_to superadmin_dashboard_path, class: "sidebar-link #{'active' if controller_name == 'dashboard' && controller_path.start_with?('superadmin')}" do %>
                  <span class="icon">📊</span> <span>Panel Universo</span>
                <% end %>
                <%= link_to superadmin_companies_path, class: "sidebar-link #{'active' if controller_name == 'companies' && controller_path.start_with?('superadmin')}" do %>
                  <span class="icon">🏢</span> <span>Empresas</span>
                <% end %>
                <%= link_to superadmin_subscription_payments_path, class: "sidebar-link #{'active' if controller_name == 'subscription_payments' && controller_path.start_with?('superadmin')}" do %>
                  <span class="icon">💳</span> <span>Pagos Manuales</span>
                  <% 
                    pending_sub_count = SubscriptionPayment.pending.count rescue 0
                  %>
                  <% if pending_sub_count > 0 %>
                    <span style="background: #ec4899; color: white; border-radius: 99px; font-size: 0.72em; padding: 2px 7px; font-weight: 800; margin-left: auto;">
                      <%= pending_sub_count %>
                    </span>
                  <% end %>
                <% end %>
              </div>
            <% end %>


            <%= link_to root_path, class: "sidebar-link #{'active' if controller_name == 'pages' && action_name == 'dashboard'}" do %>
              <span class="icon">🏠</span> <span>Inicio (Dashboard)</span>
            <% end %>

            <% if current_user.leader? || current_user.staff? || current_user.musician? || current_user.superadmin? %>
              <%= link_to notifications_path, class: "sidebar-link #{'active' if controller_name == 'notifications'}" do %>
                <span class="icon">🔔</span> <span>Notificaciones</span>
                <%= render "notifications/unread_badge" %>
              <% end %>
            <% end %>

            <% if current_user.leader? || current_user.superadmin? %>

              <%= link_to items_path, class: "sidebar-link #{'active' if controller_name == 'items'}" do %>
                <span class="icon">📦</span> <span>Inventario</span>
              <% end %>

              <%= link_to shopping_items_path, class: "sidebar-link #{'active' if controller_name == 'shopping_items'}" do %>
                <span class="icon">🛒</span> <span>Lista de Compras</span>
              <% end %>

              <%= link_to clients_path, class: "sidebar-link #{'active' if controller_name == 'clients' && action_name == 'index'}" do %>
                <span class="icon">👥</span> <span>Clientes</span>
              <% end %>

              <%= link_to debts_clients_path, class: "sidebar-link #{'active' if controller_name == 'clients' && action_name == 'debts'}" do %>
                <span class="icon">💳</span> <span>Cuentas por Cobrar</span>
              <% end %>

              <%= link_to preset_budgets_path, class: "sidebar-link #{'active' if controller_name == 'preset_budgets'}" do %>
                <span class="icon">📄</span> <span>Presupuestos Base</span>
              <% end %>

              <%= link_to client_quotes_path, class: "sidebar-link #{'active' if controller_name == 'client_quotes'}" do %>
                <span class="icon">📋</span> <span>Presupuestos Clientes</span>
              <% end %>

              <%= link_to payment_methods_settings_path, class: "sidebar-link #{'active' if controller_name == 'payment_methods'}" do %>
                <span class="icon">💳</span> <span>Métodos de Pago</span>
              <% end %>

              <%= link_to standard_upsells_path, class: "sidebar-link #{'active' if controller_name == 'standard_upsells'}" do %>
                <span class="icon">🚀</span> <span>Adicionales (Upsells)</span>
              <% end %>

              <% if current_company.present? %>
                <%= link_to subscriptions_path, class: "sidebar-link #{'active' if controller_name == 'subscriptions'}" do %>
                  <span class="icon">💳</span> <span>Suscripción SaaS</span>
                  <% if current_company.trial_active? %>
                    <span style="background: rgba(139, 92, 246, 0.3); color: #c084fc; border: 1px solid rgba(139, 92, 246, 0.4); border-radius: 99px; font-size: 0.72em; padding: 2px 7px; font-weight: 800; margin-left: auto;">
                      Trial <%= current_company.days_left_in_trial %>d
                    </span>
                  <% end %>
                <% end %>
              <% end %>

              <%= link_to gigs_path, class: "sidebar-link #{'active' if controller_name == 'gigs' && action_name != 'my'}" do %>
                <span class="icon">🎸</span> <span>Gigs / Eventos</span>
                <% 
                  pending_upsells_count = current_company&.gig_upsell_requests&.pending&.count rescue 0 
                %>
                <% if pending_upsells_count.to_i > 0 %>
                  <span style="background: #3b82f6; color: white; border-radius: 99px; font-size: 0.72em; padding: 2px 7px; font-weight: 800; margin-left: auto;">
                    <%= pending_upsells_count %>
                  </span>
                <% end %>
              <% end %>

              <%= link_to gig_payments_path, class: "sidebar-link #{'active' if controller_name == 'gig_payments'}" do %>
                <span class="icon">💳</span> <span>Pagos Eventos / Shows</span>
              <% end %>

              <%= link_to kits_path, class: "sidebar-link #{'active' if controller_name == 'kits'}" do %>
                <span class="icon">🛠️</span> <span>Plantillas (Kits)</span>
              <% end %>

              <%= link_to maintenance_records_path, class: "sidebar-link #{'active' if controller_name == 'maintenance_records'}" do %>
                <span class="icon">🔧</span> <span>Taller (Reparos)</span>
              <% end %>

              <%= link_to availability_dashboard_path, class: "sidebar-link #{'active' if controller_name == 'pages' && action_name == 'availability'}" do %>
                <span class="icon">⚡</span> <span>Conflictos</span>
              <% end %>

              <%= link_to financials_dashboard_path, class: "sidebar-link #{'active' if controller_name == 'pages' && action_name == 'financials'}" do %>
                <span class="icon">📊</span> <span>Métricas / Finanzas</span>
              <% end %>

              <%= link_to investments_path, class: "sidebar-link #{'active' if controller_name == 'investments'}" do %>
                <span class="icon">💸</span> <span>Inversiones</span>
              <% end %>

              <%= link_to employee_payments_path, class: "sidebar-link #{'active' if controller_name == 'employee_payments'}" do %>
                <span class="icon">💰</span> <span>Pagos a trabajadores</span>
                <% 
                  pending_count = current_company&.employee_payments&.pending_approval&.count rescue 0 
                %>
                <% if pending_count.to_i > 0 %>
                  <span style="background: #f59e0b; color: white; border-radius: 99px; font-size: 0.72em; padding: 2px 7px; font-weight: 800; margin-left: auto;">
                    <%= pending_count %>
                  </span>
                <% end %>
              <% end %>

              <%= link_to users_path, class: "sidebar-link #{'active' if controller_name == 'users'}" do %>
                <span class="icon">⚙️</span> <span>Roles / Accesos</span>
              <% end %>

              <%= link_to normativas_path, class: "sidebar-link #{'active' if controller_name == 'pages' && action_name == 'normativas'}" do %>
                <span class="icon">📜</span> <span>Normativas</span>
              <% end %>

            <% elsif current_user.staff? %>
              <div class="nav-header">Agrupación</div>
              <%= link_to my_gigs_path, class: "sidebar-link #{'active' if controller_name == 'gigs' && action_name == 'my'}" do %>
                <span class="icon">🎫</span> <span>Mis eventos</span>
              <% end %>

              <%= link_to my_payments_path, class: "sidebar-link #{'active' if controller_name == 'pages' && action_name == 'my_payments'}" do %>
                <span class="icon">💵</span> <span>Mis Pagos</span>
              <% end %>

              <%= link_to edit_user_path(current_user), class: "sidebar-link #{'active' if controller_path == 'users' && action_name == 'edit'}" do %>
                <span class="icon">👤</span> <span>Mi Perfil</span>
              <% end %>

              <%= link_to help_path, class: "sidebar-link #{'active' if controller_name == 'pages' && action_name == 'help'}" do %>
                <span class="icon">❓</span> <span>Ayuda</span>
              <% end %>
            <% elsif current_user.musician? %>
              <div class="nav-header">Músico</div>
              <%= link_to my_gigs_path, class: "sidebar-link #{'active' if controller_name == 'gigs' && action_name == 'my'}" do %>
                <span class="icon">🎸</span> <span>Mis Shows</span>
              <% end %>

              <%= link_to my_payments_path, class: "sidebar-link #{'active' if controller_name == 'pages' && action_name == 'my_payments'}" do %>
                <span class="icon">💵</span> <span>Mis Pagos</span>
              <% end %>

              <%= link_to edit_user_path(current_user), class: "sidebar-link #{'active' if controller_path == 'users' && action_name == 'edit'}" do %>
                <span class="icon">👤</span> <span>Mi Perfil</span>
              <% end %>

              <%= link_to help_path, class: "sidebar-link #{'active' if controller_name == 'pages' && action_name == 'help'}" do %>
                <span class="icon">❓</span> <span>Ayuda</span>
              <% end %>
            <% elsif current_user.client? %>
              <div class="nav-header">Cliente</div>
              <%= link_to client_gigs_path, class: "sidebar-link #{'active' if controller_path == 'client/gigs' && action_name == 'index'}" do %>
                <span class="icon">🎫</span> <span>Mis Eventos</span>
              <% end %>

              <%# Acceso rápido al próximo evento %>
              <%
                next_gig = current_user.client&.gigs&.where('date >= ?', Date.today)&.order(date: :asc)&.first
              %>
              <% if next_gig %>
                <%= link_to client_gig_path(next_gig), class: "sidebar-link #{'active' if controller_path == 'client/gigs' && action_name == 'show'}" do %>
                  <span class="icon">⭐</span> <span>Mi Próximo Show</span>
                <% end %>
              <% end %>

              <%= link_to preset_budgets_path, class: "sidebar-link #{'active' if controller_name == 'preset_budgets'}" do %>
                <span class="icon">📋</span> <span>Presupuestos Base</span>
              <% end %>

              <%= link_to normativas_path, class: "sidebar-link #{'active' if controller_name == 'pages' && action_name == 'normativas'}" do %>
                <span class="icon">📜</span> <span>Normativas</span>
              <% end %>

              <%= link_to edit_user_path(current_user), class: "sidebar-link #{'active' if controller_path == 'users' && action_name == 'edit'}" do %>
                <span class="icon">👤</span> <span>Mi Perfil</span>
              <% end %>

              <%= link_to help_path, class: "sidebar-link #{'active' if controller_name == 'pages' && action_name == 'help'}" do %>
                <span class="icon">❓</span> <span>Ayuda</span>
              <% end %>
            <% end %>
            <div id="sidebar-scroll-indicator" aria-hidden="true"></div>
          </nav>

          <%# Pie del Sidebar: Usuario y Logout %>
          <div class="sidebar-footer">
            <div class="user-info">
              <div class="avatar" style="overflow: hidden; display: flex; align-items: center; justify-content: center;">
                <% if current_user.avatar_attached? %>
                  <%= image_tag current_user.avatar_url_or_data, style: "width: 100%; height: 100%; object-fit: cover; border-radius: 50%;" %>
                <% else %>
                  <%= current_user.email[0].upcase %>
                <% end %>
              </div>
              <div class="user-details">
                <p class="email"><%= current_user.display_name %></p>
                <p class="status">En línea</p>
              </div>
            </div>
            <%= button_to "Cerrar Sesión", destroy_user_session_path, method: :delete, class: "btn-logout" %>
          </div>
        </aside>
      <% end %>

      <%# --- CONTENIDO PRINCIPAL --- %>
      <main style="<%= user_signed_in? ? 'margin-left: 280px;' : 'margin-left: 0;' %> flex-grow: 1; min-height: 100vh; padding: 40px; box-sizing: border-box; overflow-x: auto;">
        <div style="max-width: 1200px; margin: 0 auto;">
          
          <% if user_signed_in? && current_user.superadmin? && session[:superadmin_company_id].present? %>
            <div style="background: linear-gradient(135deg, #4338ca, #3730a3); color: white; padding: 12px 20px; border-radius: 12px; margin-bottom: 24px; display: flex; align-items: center; justify-content: space-between; flex-wrap: wrap; gap: 12px; box-shadow: 0 4px 12px rgba(67, 56, 202, 0.3);">
              <div style="display: flex; align-items: center; gap: 10px; font-size: 0.92rem; font-weight: 600;">
                <span style="font-size: 1.2rem;">👁️</span>
                <span>Modo Inspección Activo: Estás viendo la plataforma como <strong><%= current_company&.name %></strong>.</span>
              </div>
              <%= button_to switch_tenant_superadmin_company_path("all"), method: :post, style: "background: rgba(255,255,255,0.2); border: 1px solid rgba(255,255,255,0.3); color: white; padding: 6px 14px; border-radius: 8px; font-weight: 700; font-size: 0.82rem; cursor: pointer;" do %>
                Volver a Modo Universo Superadmin
              <% end %>
            </div>
          <% end %>

          <% if user_signed_in? && (current_user.leader? || current_user.superadmin?) && controller_name != 'employee_payments' && !(controller_name == 'pages' && action_name == 'dashboard') %>
            <% pending_worker_reports = current_company&.employee_payments&.pending_approval&.count rescue 0 %>
            <% if pending_worker_reports.to_i > 0 %>
              <div style="background: #fffbeb; border: 1px solid #fde68a; border-radius: 14px; padding: 14px 20px; margin-bottom: 24px; display: flex; justify-content: space-between; align-items: center; gap: 14px; flex-wrap: wrap; box-shadow: 0 2px 8px rgba(245, 158, 11, 0.08);">
                <div style="display: flex; align-items: center; gap: 10px; color: #92400e; font-size: 0.92rem; font-weight: 600;">
                  <span style="font-size: 1.3rem;">🔔</span>
                  <span><strong>Notificación:</strong> Tienes <strong><%= pending_worker_reports %> pago(s) reportado(s)</strong> por trabajadores esperando tu confirmación.</span>
                </div>
                <%= link_to 'Revisar y Confirmar ➔', employee_payments_path, style: 'background: #f59e0b; color: white; padding: 7px 16px; border-radius: 8px; font-weight: 700; font-size: 0.84rem; text-decoration: none; box-shadow: 0 2px 4px rgba(245, 158, 11, 0.25); white-space: nowrap;' %>
              </div>
            <% end %>

            <% pending_upsells = (current_company&.gig_upsell_requests&.pending&.includes(:gig)&.limit(10)&.to_a rescue []) || [] %>
            <% pending_upsell_requests_count = pending_upsells.size %>
            <% if pending_upsell_requests_count.positive? && !(controller_name == 'gigs' && action_name == 'show') %>
              <% first_pending_gig = pending_upsells.first&.gig %>
              <div style="background: #eff6ff; border: 1px solid #bfdbfe; border-radius: 14px; padding: 14px 20px; margin-bottom: 24px; display: flex; justify-content: space-between; align-items: center; gap: 14px; flex-wrap: wrap; box-shadow: 0 2px 8px rgba(59, 130, 246, 0.08);">
                <div style="display: flex; align-items: center; gap: 10px; color: #1e40af; font-size: 0.92rem; font-weight: 600;">
                  <span style="font-size: 1.3rem;">🚀</span>
                  <span><strong>Aviso de Clientes:</strong> Tienes <strong><%= pending_upsell_requests_count %> solicitud(es) de adicionales (upsells)</strong> de clientes esperando tu decisión.</span>
                </div>
                <% if first_pending_gig %>
                  <%= link_to 'Ver y Decidir en el Evento ➔', gig_path(first_pending_gig), style: 'background: #2563eb; color: white; padding: 7px 16px; border-radius: 8px; font-weight: 700; font-size: 0.84rem; text-decoration: none; box-shadow: 0 2px 4px rgba(37, 99, 235, 0.25); white-space: nowrap;' %>
                <% else %>
                  <%= link_to 'Ver Eventos ➔', gigs_path, style: 'background: #2563eb; color: white; padding: 7px 16px; border-radius: 8px; font-weight: 700; font-size: 0.84rem; text-decoration: none; box-shadow: 0 2px 4px rgba(37, 99, 235, 0.25); white-space: nowrap;' %>
                <% end %>
              </div>
            <% end %>
          <% end %>

          <%# ── Flash notices ── %>

          <% if notice.present? %>
            <script>
              document.addEventListener('turbo:load', function() {
                showToast(<%= notice.to_json %>, 'success');
              }, { once: true });
            </script>
          <% end %>
          <% if alert.present? %>
            <script>
              document.addEventListener('turbo:load', function() {
                showToast(<%= alert.to_json %>, 'error');
              }, { once: true });
            </script>
          <% end %>

          <%= yield %>
        </div>
      </main>


    </div>

    <script>
      /* ── Toast notification system ───────────────────────── */
      function showToast(message, type) {
        var container = document.getElementById('toast-container');
        if (!container) return;

        var isSuccess = type === 'success';

        // Play sound if PWA sound engine is active
        if (typeof playNotificationSound === 'function') {
          playNotificationSound(isSuccess ? 'default' : 'urgent');
        }

        var toast = document.createElement('div');
        toast.style.cssText = [
          'pointer-events: all',
          'display: flex',
          'align-items: flex-start',
          'gap: 10px',
          'padding: 14px 16px',
          'border-radius: 14px',
          'font-family: Inter, sans-serif',
          'font-size: 0.9rem',
          'font-weight: 600',
          'line-height: 1.4',
          'box-shadow: 0 8px 30px rgba(0,0,0,0.14)',
          'border: 1px solid ' + (isSuccess ? '#86efac' : '#fca5a5'),
          'background: ' + (isSuccess ? '#f0fdf4' : '#fff1f2'),
          'color: ' + (isSuccess ? '#15803d' : '#9f1239'),
          'transform: translateX(120%)',
          'transition: transform 0.3s cubic-bezier(0.34,1.56,0.64,1)',
          'position: relative',
          'overflow: hidden',
          'max-width: 100%',
        ].join(';');

        toast.innerHTML =
          '<span style="font-size:1.2em;flex-shrink:0;">' + (isSuccess ? '✅' : '⚠️') + '</span>' +
          '<span style="flex:1;">' + message + '</span>' +
          '<button onclick="this.closest(\'[data-toast]\').remove()" style="background:none;border:none;cursor:pointer;font-size:1.1em;color:inherit;opacity:0.6;padding:0;margin:0;line-height:1;flex-shrink:0;" title="Cerrar">✕</button>' +
          '<div style="position:absolute;bottom:0;left:0;height:3px;background:' + (isSuccess ? '#22c55e' : '#f87171') + ';width:100%;transform-origin:left;animation:toastProgress 4s linear forwards;border-radius:0 0 14px 14px;"></div>';
        toast.setAttribute('data-toast', '1');

        container.appendChild(toast);
        requestAnimationFrame(function() {
          requestAnimationFrame(function() {
            toast.style.transform = 'translateX(0)';
          });
        });

        var timer = setTimeout(function() { dismissToast(toast); }, 4000);
        toast.addEventListener('mouseenter', function() { clearTimeout(timer); });
        toast.addEventListener('mouseleave', function() { timer = setTimeout(function() { dismissToast(toast); }, 1500); });
      }

      function dismissToast(toast) {
        if (!toast || !toast.parentNode) return;
        toast.style.transform = 'translateX(120%)';
        toast.style.opacity = '0';
        setTimeout(function() { if (toast.parentNode) toast.parentNode.removeChild(toast); }, 350);
      }

      function updateUnreadBadge(delta) {
        var badges = [
          document.getElementById('unread-notifications-badge'),
          document.getElementById('mobile-topbar-unread-badge')
        ];
        badges.forEach(function(badge) {
          if (!badge) return;
          var currentCount = parseInt(badge.getAttribute('data-count') || badge.textContent.trim() || '0', 10);
          var newCount = Math.max(0, currentCount + delta);
          badge.setAttribute('data-count', newCount);
          badge.textContent = newCount;
          badge.style.display = newCount > 0 ? (badge.id === 'unread-notifications-badge' ? 'inline-block' : 'inline-block') : 'none';
        });
      }

      /* ── Prevent Turbo scroll-to-top on form actions ─────── */
      (function() {
        var savedScrollY = 0;

        // Guardar posición antes de que Turbo procese la respuesta
        document.addEventListener('turbo:before-fetch-response', function() {
          savedScrollY = window.scrollY;
        });

        // Después de que Turbo actualiza el DOM, restaurar posición
        document.addEventListener('turbo:render', function() {
          if (savedScrollY > 0) {
            window.scrollTo({ top: savedScrollY, behavior: 'instant' });
          }
        });
      })();

      /* ── Turbo compatibility ─────────────────────────────── */
      document.addEventListener('turbo:load', function() {
        // container exists after SPA nav
      });

      (function() {
        // ─── Scroll indicator (original) ───────────────────────────
        document.addEventListener('DOMContentLoaded', function(){
          var nav = document.querySelector('aside nav');
          var indicator = document.getElementById('sidebar-scroll-indicator');
          if(nav && indicator) {
            function refresh(){
              var scrollHeight = nav.scrollHeight;
              var client = nav.clientHeight;
              if(scrollHeight <= client){ indicator.style.opacity = '0'; return; }
              indicator.style.opacity = '0.9';
              var thumbPct = (client / scrollHeight) * 100;
              var ratio = nav.scrollTop / (scrollHeight - client);
              var translatePct = ratio * (100 - thumbPct);
              indicator.style.height = thumbPct + '%';
              indicator.style.top = translatePct + '%';
            }
            nav.addEventListener('scroll', refresh, { passive: true });
            window.addEventListener('resize', refresh);
            refresh();
          }
        });

        // ─── Mobile drawer ──────────────────────────────────────────
        function initDrawer() {
          var sidebar   = document.getElementById('app-sidebar');
          var hamburger = document.getElementById('hamburger-btn');
          var closeBtn  = document.getElementById('sidebar-close-btn');
          var backdrop  = document.getElementById('mobile-backdrop');
          if (!sidebar) return;

          function open() {
            sidebar.classList.add('sidebar-open');
            if (backdrop) backdrop.classList.add('open');
            if (hamburger) hamburger.setAttribute('aria-expanded', 'true');
            document.body.style.overflow = 'hidden';
          }
          function close() {
            sidebar.classList.remove('sidebar-open');
            if (backdrop) backdrop.classList.remove('open');
            if (hamburger) hamburger.setAttribute('aria-expanded', 'false');
            document.body.style.overflow = '';
          }

          if (hamburger) hamburger.addEventListener('click', open);
          if (closeBtn)  closeBtn.addEventListener('click', close);
          if (backdrop)  backdrop.addEventListener('click', close);

          // Cerrar al navegar en móvil
          sidebar.querySelectorAll('.sidebar-link').forEach(function(link) {
            link.addEventListener('click', function() {
              if (window.innerWidth <= 991) close();
            });
          });

          // Cerrar drawer si se redimensiona a desktop
          window.addEventListener('resize', function() {
            if (window.innerWidth > 991) close();
          });
        }

        document.addEventListener('DOMContentLoaded', initDrawer);
        document.addEventListener('turbo:load', initDrawer);
      })();
    </script>

    <%# ── TOAST CONTAINER (Root level para visibilidad global) ── %>
    <div id="toast-container" style="position:fixed; top:20px; right:20px; z-index:999999; display:flex; flex-direction:column; gap:10px; pointer-events:none; max-width:400px; width:calc(100vw - 40px);"></div>

    <%# ── BANNER DE INSTALACIÓN PWA (Android / Desktop) ── %>
    <div id="pwa-install-banner" style="display: none; position: fixed; bottom: 20px; left: 16px; right: 16px; max-width: 440px; margin: 0 auto; background: #0f172a; border: 1px solid #334155; border-radius: 18px; padding: 14px 18px; box-shadow: 0 16px 36px rgba(0,0,0,0.45); z-index: 10000; align-items: center; justify-content: space-between; gap: 12px; transform: translateY(100%); opacity: 0; transition: transform 0.3s cubic-bezier(0.16, 1, 0.3, 1), opacity 0.3s ease; color: white; font-family: 'Inter', system-ui, sans-serif;">
      <div style="display: flex; align-items: center; gap: 12px; min-width: 0;">
        <img src="/icons/icon-192.png" alt="Gig Manager" style="width: 42px; height: 42px; border-radius: 10px; flex-shrink: 0; box-shadow: 0 4px 12px rgba(59,130,246,0.3);">
        <div style="min-width: 0;">
          <div style="font-weight: 800; font-size: 0.92rem; color: #ffffff; white-space: nowrap; overflow: hidden; text-overflow: ellipsis;">Instalar Gig Manager</div>
          <div style="font-size: 0.78rem; color: #94a3b8;">Acceso rápido sin navegador</div>
        </div>
      </div>
      <div style="display: flex; align-items: center; gap: 8px; flex-shrink: 0;">
        <button onclick="promptPWAInstall()" style="background: linear-gradient(135deg, #3b82f6, #2563eb); color: white; border: none; padding: 8px 14px; border-radius: 10px; font-weight: 700; font-size: 0.85rem; cursor: pointer; box-shadow: 0 4px 12px rgba(37,99,235,0.35);">
          Instalar
        </button>
        <button onclick="hideInstallBanner(true)" style="background: transparent; border: none; color: #64748b; font-size: 1.2rem; cursor: pointer; padding: 4px;" title="Cerrar">✕</button>
      </div>
    </div>

    <%# ── MODAL GUÍA DE INSTALACIÓN MULTIPLATAFORMA (PC, iPhone, Android) ── %>
    <div id="pwa-install-guide-modal" style="display: none; position: fixed; inset: 0; background: rgba(15, 23, 42, 0.75); backdrop-filter: blur(4px); -webkit-backdrop-filter: blur(4px); z-index: 10001; align-items: center; justify-content: center; padding: 16px; opacity: 0; transition: opacity 0.25s ease; font-family: 'Inter', system-ui, sans-serif;">
      <div style="background: #1e293b; border: 1px solid #334155; border-radius: 24px; padding: 24px; width: 100%; max-width: 480px; color: white; box-shadow: 0 25px 50px -12px rgba(0, 0, 0, 0.5); max-height: 90vh; overflow-y: auto;">
        
        <div style="display: flex; align-items: center; justify-content: space-between; margin-bottom: 16px;">
          <div style="display: flex; align-items: center; gap: 10px;">
            <img src="/icons/icon-192.png" alt="Gig Manager" style="width: 40px; height: 40px; border-radius: 10px; box-shadow: 0 4px 12px rgba(59,130,246,0.3);">
            <div>
              <h3 style="margin: 0; font-size: 1.15rem; font-weight: 800; color: #ffffff;">Instalar Gig Manager</h3>
              <p style="margin: 0; font-size: 0.78rem; color: #94a3b8;">Acceso directo como app nativa</p>
            </div>
          </div>
          <button onclick="hideInstallGuideModal()" style="background: rgba(255,255,255,0.08); border: none; border-radius: 50%; width: 32px; height: 32px; color: #94a3b8; font-size: 1.1rem; cursor: pointer; display: flex; align-items: center; justify-content: center;">✕</button>
        </div>

        <%# Selector de dispositivo %>
        <div style="display: flex; gap: 6px; background: rgba(255,255,255,0.05); padding: 4px; border-radius: 12px; margin-bottom: 18px;">
          <button id="tab-btn-pc" onclick="switchInstallTab('pc')" style="flex: 1; padding: 8px 6px; border-radius: 8px; border: none; font-size: 0.8rem; font-weight: 700; cursor: pointer; background: #3b82f6; color: white;">💻 PC / Mac</button>
          <button id="tab-btn-ios" onclick="switchInstallTab('ios')" style="flex: 1; padding: 8px 6px; border-radius: 8px; border: none; font-size: 0.8rem; font-weight: 700; cursor: pointer; background: transparent; color: #94a3b8;">🍏 iPhone</button>
          <button id="tab-btn-android" onclick="switchInstallTab('android')" style="flex: 1; padding: 8px 6px; border-radius: 8px; border: none; font-size: 0.8rem; font-weight: 700; cursor: pointer; background: transparent; color: #94a3b8;">🤖 Android</button>
        </div>

        <%# Contenido Tab PC %>
        <div id="tab-content-pc" style="display: block;">
          <p style="color: #cbd5e1; font-size: 0.88rem; line-height: 1.45; margin-bottom: 16px;">
            En tu computadora (Chrome, Edge o Safari en Mac):
          </p>
          <div style="display: flex; flex-direction: column; gap: 12px; margin-bottom: 20px;">
            <div style="display: flex; align-items: center; gap: 12px; background: rgba(255,255,255,0.04); padding: 12px 14px; border-radius: 12px; border: 1px solid rgba(255,255,255,0.06);">
              <div style="font-size: 1rem; font-weight: 800; background: #334155; width: 30px; height: 30px; border-radius: 8px; display: flex; align-items: center; justify-content: center; flex-shrink: 0; color: #60a5fa;">1</div>
              <div style="font-size: 0.86rem; color: #f1f5f9;">Haz clic en el icono <strong>Instalar (⊕ o 📲)</strong> en la barra de direcciones superior del navegador.</div>
            </div>
            <div style="display: flex; align-items: center; gap: 12px; background: rgba(255,255,255,0.04); padding: 12px 14px; border-radius: 12px; border: 1px solid rgba(255,255,255,0.06);">
              <div style="font-size: 1rem; font-weight: 800; background: #334155; width: 30px; height: 30px; border-radius: 8px; display: flex; align-items: center; justify-content: center; flex-shrink: 0; color: #60a5fa;">2</div>
              <div style="font-size: 0.86rem; color: #f1f5f9;">O abre el menú <strong>(⋮)</strong> de tu navegador y selecciona <strong>"Instalar Gig Manager"</strong>.</div>
            </div>
          </div>
        </div>

        <%# Contenido Tab iOS %>
        <div id="tab-content-ios" style="display: none;">
          <p style="color: #cbd5e1; font-size: 0.88rem; line-height: 1.45; margin-bottom: 16px;">
            En tu iPhone o iPad (usando Safari):
          </p>
          <div style="display: flex; flex-direction: column; gap: 12px; margin-bottom: 20px;">
            <div style="display: flex; align-items: center; gap: 12px; background: rgba(255,255,255,0.04); padding: 12px 14px; border-radius: 12px; border: 1px solid rgba(255,255,255,0.06);">
              <div style="font-size: 1rem; font-weight: 800; background: #334155; width: 30px; height: 30px; border-radius: 8px; display: flex; align-items: center; justify-content: center; flex-shrink: 0; color: #60a5fa;">1</div>
              <div style="font-size: 0.86rem; color: #f1f5f9;">Toca el botón <strong>Compartir</strong> (icono <span style="font-size: 1.1rem;">⎋</span> o flecha arriba) en Safari.</div>
            </div>
            <div style="display: flex; align-items: center; gap: 12px; background: rgba(255,255,255,0.04); padding: 12px 14px; border-radius: 12px; border: 1px solid rgba(255,255,255,0.06);">
              <div style="font-size: 1rem; font-weight: 800; background: #334155; width: 30px; height: 30px; border-radius: 8px; display: flex; align-items: center; justify-content: center; flex-shrink: 0; color: #60a5fa;">2</div>
              <div style="font-size: 0.86rem; color: #f1f5f9;">Desliza hacia abajo y toca <strong>"Agregar a pantalla de inicio"</strong> ➕.</div>
            </div>
            <div style="display: flex; align-items: center; gap: 12px; background: rgba(255,255,255,0.04); padding: 12px 14px; border-radius: 12px; border: 1px solid rgba(255,255,255,0.06);">
              <div style="font-size: 1rem; font-weight: 800; background: #334155; width: 30px; height: 30px; border-radius: 8px; display: flex; align-items: center; justify-content: center; flex-shrink: 0; color: #60a5fa;">3</div>
              <div style="font-size: 0.86rem; color: #f1f5f9;">Toca <strong>"Agregar"</strong> arriba a la derecha. ¡Listo! 🎉</div>
            </div>
          </div>
        </div>

        <%# Contenido Tab Android %>
        <div id="tab-content-android" style="display: none;">
          <p style="color: #cbd5e1; font-size: 0.88rem; line-height: 1.45; margin-bottom: 16px;">
            En tu teléfono Android (Chrome o Edge):
          </p>
          <div style="display: flex; flex-direction: column; gap: 12px; margin-bottom: 20px;">
            <div style="display: flex; align-items: center; gap: 12px; background: rgba(255,255,255,0.04); padding: 12px 14px; border-radius: 12px; border: 1px solid rgba(255,255,255,0.06);">
              <div style="font-size: 1rem; font-weight: 800; background: #334155; width: 30px; height: 30px; border-radius: 8px; display: flex; align-items: center; justify-content: center; flex-shrink: 0; color: #60a5fa;">1</div>
              <div style="font-size: 0.86rem; color: #f1f5f9;">Toca el menú <strong>(⋮)</strong> de tres puntos en la esquina superior de Chrome.</div>
            </div>
            <div style="display: flex; align-items: center; gap: 12px; background: rgba(255,255,255,0.04); padding: 12px 14px; border-radius: 12px; border: 1px solid rgba(255,255,255,0.06);">
              <div style="font-size: 1rem; font-weight: 800; background: #334155; width: 30px; height: 30px; border-radius: 8px; display: flex; align-items: center; justify-content: center; flex-shrink: 0; color: #60a5fa;">2</div>
              <div style="font-size: 0.86rem; color: #f1f5f9;">Selecciona <strong>"Instalar aplicación"</strong> o <strong>"Agregar a la pantalla principal"</strong>.</div>
            </div>
          </div>
        </div>

        <button onclick="hideInstallGuideModal()" style="width: 100%; background: linear-gradient(135deg, #3b82f6, #2563eb); color: white; border: none; padding: 12px; border-radius: 12px; font-weight: 700; font-size: 0.92rem; cursor: pointer;">
          Entendido
        </button>
      </div>
    </div>

    <script>
      function switchInstallTab(tabName) {
        ['pc', 'ios', 'android'].forEach(function(name) {
          var content = document.getElementById('tab-content-' + name);
          var btn = document.getElementById('tab-btn-' + name);
          if (content && btn) {
            if (name === tabName) {
              content.style.display = 'block';
              btn.style.background = '#3b82f6';
              btn.style.color = '#ffffff';
            } else {
              content.style.display = 'none';
              btn.style.background = 'transparent';
              btn.style.color = '#94a3b8';
            }
          }
        });
      }
    </script>
  </body>
</html>
lor: #60a5fa;">3</div>
            <div style="font-size: 0.86rem; color: #f1f5f9;">Toca <strong>"Agregar"</strong> en la esquina superior derecha. ¡Listo! 🎉</div>
          </div>
        </div>
        <button onclick="hideIOSInstallModal()" style="width: 100%; background: linear-gradient(135deg, #3b82f6, #2563eb); color: white; border: none; padding: 12px; border-radius: 12px; font-weight: 700; font-size: 0.92rem; cursor: pointer;">
          Entendido
        </button>
      </div>
    </div>
  </body>
</html>